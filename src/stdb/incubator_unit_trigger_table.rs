// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_unit_trigger_type::TIncubatorUnitTrigger;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_unit_trigger`.
///
/// Obtain a handle from the [`IncubatorUnitTriggerTableAccess::incubator_unit_trigger`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_unit_trigger()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit_trigger().on_insert(...)`.
pub struct IncubatorUnitTriggerTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorUnitTrigger>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_unit_trigger`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorUnitTriggerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorUnitTriggerTableHandle`], which mediates access to the table `incubator_unit_trigger`.
    fn incubator_unit_trigger(&self) -> IncubatorUnitTriggerTableHandle<'_>;
}

impl IncubatorUnitTriggerTableAccess for super::RemoteTables {
    fn incubator_unit_trigger(&self) -> IncubatorUnitTriggerTableHandle<'_> {
        IncubatorUnitTriggerTableHandle {
            imp: self
                .imp
                .get_table::<TIncubatorUnitTrigger>("incubator_unit_trigger"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorUnitTriggerInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorUnitTriggerDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorUnitTriggerTableHandle<'ctx> {
    type Row = TIncubatorUnitTrigger;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorUnitTrigger> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorUnitTriggerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitTriggerInsertCallbackId {
        IncubatorUnitTriggerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorUnitTriggerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorUnitTriggerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitTriggerDeleteCallbackId {
        IncubatorUnitTriggerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorUnitTriggerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorUnitTriggerUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorUnitTriggerTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorUnitTriggerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitTriggerUpdateCallbackId {
        IncubatorUnitTriggerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorUnitTriggerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorUnitTrigger>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorUnitTrigger| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_unit_trigger\"")
}

/// Access to the `id` unique index on the table `incubator_unit_trigger`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorUnitTriggerIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit_trigger().id().find(...)`.
pub struct IncubatorUnitTriggerIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorUnitTrigger, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorUnitTriggerTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_unit_trigger`.
    pub fn id(&self) -> IncubatorUnitTriggerIdUnique<'ctx> {
        IncubatorUnitTriggerIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorUnitTriggerIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorUnitTrigger> {
        self.imp.find(col_val)
    }
}
