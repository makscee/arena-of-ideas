// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_effect_type::TIncubatorEffect;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_effect`.
///
/// Obtain a handle from the [`IncubatorEffectTableAccess::incubator_effect`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_effect()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_effect().on_insert(...)`.
pub struct IncubatorEffectTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorEffect>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_effect`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorEffectTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorEffectTableHandle`], which mediates access to the table `incubator_effect`.
    fn incubator_effect(&self) -> IncubatorEffectTableHandle<'_>;
}

impl IncubatorEffectTableAccess for super::RemoteTables {
    fn incubator_effect(&self) -> IncubatorEffectTableHandle<'_> {
        IncubatorEffectTableHandle {
            imp: self.imp.get_table::<TIncubatorEffect>("incubator_effect"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorEffectInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorEffectDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorEffectTableHandle<'ctx> {
    type Row = TIncubatorEffect;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorEffect> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorEffectInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorEffectInsertCallbackId {
        IncubatorEffectInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorEffectInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorEffectDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorEffectDeleteCallbackId {
        IncubatorEffectDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorEffectDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorEffectUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorEffectTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorEffectUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorEffectUpdateCallbackId {
        IncubatorEffectUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorEffectUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorEffect>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorEffect| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_effect\"")
}

/// Access to the `id` unique index on the table `incubator_effect`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorEffectIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_effect().id().find(...)`.
pub struct IncubatorEffectIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorEffect, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorEffectTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_effect`.
    pub fn id(&self) -> IncubatorEffectIdUnique<'ctx> {
        IncubatorEffectIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorEffectIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorEffect> {
        self.imp.find(col_val)
    }
}
