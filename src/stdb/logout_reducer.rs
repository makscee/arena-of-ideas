// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LogoutArgs {}

impl From<LogoutArgs> for super::Reducer {
    fn from(args: LogoutArgs) -> Self {
        Self::Logout
    }
}

impl __sdk::InModule for LogoutArgs {
    type Module = super::RemoteModule;
}

pub struct LogoutCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `logout`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait logout {
    /// Request that the remote module invoke the reducer `logout` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_logout`] callbacks.
    fn logout(&self) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `logout`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`LogoutCallbackId`] can be passed to [`Self::remove_on_logout`]
    /// to cancel the callback.
    fn on_logout(
        &self,
        callback: impl FnMut(&super::ReducerEventContext) + Send + 'static,
    ) -> LogoutCallbackId;
    /// Cancel a callback previously registered by [`Self::on_logout`],
    /// causing it not to run in the future.
    fn remove_on_logout(&self, callback: LogoutCallbackId);
}

impl logout for super::RemoteReducers {
    fn logout(&self) -> __sdk::Result<()> {
        self.imp.call_reducer("logout", LogoutArgs {})
    }
    fn on_logout(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext) + Send + 'static,
    ) -> LogoutCallbackId {
        LogoutCallbackId(self.imp.on_reducer(
            "logout",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::Logout {},
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx)
            }),
        ))
    }
    fn remove_on_logout(&self, callback: LogoutCallbackId) {
        self.imp.remove_on_reducer("logout", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `logout`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_logout {
    /// Set the call-reducer flags for the reducer `logout` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn logout(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_logout for super::SetReducerFlags {
    fn logout(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("logout", flags);
    }
}
