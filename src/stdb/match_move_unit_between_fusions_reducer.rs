// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchMoveUnitBetweenFusionsArgs {
    pub source_fusion_id: u64,
    pub target_fusion_id: u64,
    pub unit_id: u64,
    pub target_slot_idx: u32,
}

impl From<MatchMoveUnitBetweenFusionsArgs> for super::Reducer {
    fn from(args: MatchMoveUnitBetweenFusionsArgs) -> Self {
        Self::MatchMoveUnitBetweenFusions {
            source_fusion_id: args.source_fusion_id,
            target_fusion_id: args.target_fusion_id,
            unit_id: args.unit_id,
            target_slot_idx: args.target_slot_idx,
        }
    }
}

impl __sdk::InModule for MatchMoveUnitBetweenFusionsArgs {
    type Module = super::RemoteModule;
}

pub struct MatchMoveUnitBetweenFusionsCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_move_unit_between_fusions`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_move_unit_between_fusions {
    /// Request that the remote module invoke the reducer `match_move_unit_between_fusions` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_match_move_unit_between_fusions`] callbacks.
    fn match_move_unit_between_fusions(
        &self,
        source_fusion_id: u64,
        target_fusion_id: u64,
        unit_id: u64,
        target_slot_idx: u32,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `match_move_unit_between_fusions`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`MatchMoveUnitBetweenFusionsCallbackId`] can be passed to [`Self::remove_on_match_move_unit_between_fusions`]
    /// to cancel the callback.
    fn on_match_move_unit_between_fusions(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &u64, &u64, &u32) + Send + 'static,
    ) -> MatchMoveUnitBetweenFusionsCallbackId;
    /// Cancel a callback previously registered by [`Self::on_match_move_unit_between_fusions`],
    /// causing it not to run in the future.
    fn remove_on_match_move_unit_between_fusions(
        &self,
        callback: MatchMoveUnitBetweenFusionsCallbackId,
    );
}

impl match_move_unit_between_fusions for super::RemoteReducers {
    fn match_move_unit_between_fusions(
        &self,
        source_fusion_id: u64,
        target_fusion_id: u64,
        unit_id: u64,
        target_slot_idx: u32,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "match_move_unit_between_fusions",
            MatchMoveUnitBetweenFusionsArgs {
                source_fusion_id,
                target_fusion_id,
                unit_id,
                target_slot_idx,
            },
        )
    }
    fn on_match_move_unit_between_fusions(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &u64, &u64, &u32) + Send + 'static,
    ) -> MatchMoveUnitBetweenFusionsCallbackId {
        MatchMoveUnitBetweenFusionsCallbackId(self.imp.on_reducer(
            "match_move_unit_between_fusions",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::MatchMoveUnitBetweenFusions {
                                    source_fusion_id,
                                    target_fusion_id,
                                    unit_id,
                                    target_slot_idx,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(
                    ctx,
                    source_fusion_id,
                    target_fusion_id,
                    unit_id,
                    target_slot_idx,
                )
            }),
        ))
    }
    fn remove_on_match_move_unit_between_fusions(
        &self,
        callback: MatchMoveUnitBetweenFusionsCallbackId,
    ) {
        self.imp
            .remove_on_reducer("match_move_unit_between_fusions", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `match_move_unit_between_fusions`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_match_move_unit_between_fusions {
    /// Set the call-reducer flags for the reducer `match_move_unit_between_fusions` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn match_move_unit_between_fusions(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_match_move_unit_between_fusions for super::SetReducerFlags {
    fn match_move_unit_between_fusions(&self, flags: __ws::CallReducerFlags) {
        self.imp
            .set_call_reducer_flags("match_move_unit_between_fusions", flags);
    }
}
