// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_status_type::TStatus;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `status`.
///
/// Obtain a handle from the [`StatusTableAccess::status`] method on [`super::RemoteTables`],
/// like `ctx.db.status()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.status().on_insert(...)`.
pub struct StatusTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TStatus>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `status`.
///
/// Implemented for [`super::RemoteTables`].
pub trait StatusTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`StatusTableHandle`], which mediates access to the table `status`.
    fn status(&self) -> StatusTableHandle<'_>;
}

impl StatusTableAccess for super::RemoteTables {
    fn status(&self) -> StatusTableHandle<'_> {
        StatusTableHandle {
            imp: self.imp.get_table::<TStatus>("status"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct StatusInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct StatusDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for StatusTableHandle<'ctx> {
    type Row = TStatus;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TStatus> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = StatusInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> StatusInsertCallbackId {
        StatusInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: StatusInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = StatusDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> StatusDeleteCallbackId {
        StatusDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: StatusDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct StatusUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for StatusTableHandle<'ctx> {
    type UpdateCallbackId = StatusUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> StatusUpdateCallbackId {
        StatusUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: StatusUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TStatus>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<String>(
        raw_updates,
        |row: &TStatus| &row.name,
    )
    .context("Failed to parse table update for table \"status\"")
}

/// Access to the `name` unique index on the table `status`,
/// which allows point queries on the field of the same name
/// via the [`StatusNameUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.status().name().find(...)`.
pub struct StatusNameUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TStatus, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> StatusTableHandle<'ctx> {
    /// Get a handle on the `name` unique index on the table `status`.
    pub fn name(&self) -> StatusNameUnique<'ctx> {
        StatusNameUnique {
            imp: self
                .imp
                .get_unique_constraint::<String>("name", |row| &row.name),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> StatusNameUnique<'ctx> {
    /// Find the subscribed row whose `name` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TStatus> {
        self.imp.find(col_val)
    }
}
