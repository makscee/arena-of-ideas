// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct DismantleHero {
    pub item: u64,
}

impl __sdk::spacetime_module::InModule for DismantleHero {
    type Module = super::RemoteModule;
}

pub struct DismantleHeroCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `dismantle_hero`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait dismantle_hero {
    /// Request that the remote module invoke the reducer `dismantle_hero` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_dismantle_hero`] callbacks.
    fn dismantle_hero(&self, item: u64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `dismantle_hero`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`DismantleHeroCallbackId`] can be passed to [`Self::remove_on_dismantle_hero`]
    /// to cancel the callback.
    fn on_dismantle_hero(
        &self,
        callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> DismantleHeroCallbackId;
    /// Cancel a callback previously registered by [`Self::on_dismantle_hero`],
    /// causing it not to run in the future.
    fn remove_on_dismantle_hero(&self, callback: DismantleHeroCallbackId);
}

impl dismantle_hero for super::RemoteReducers {
    fn dismantle_hero(&self, item: u64) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("dismantle_hero", DismantleHero { item })
    }
    fn on_dismantle_hero(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> DismantleHeroCallbackId {
        DismantleHeroCallbackId(self.imp.on_reducer::<DismantleHero>(
            "dismantle_hero",
            Box::new(move |ctx: &super::EventContext, args: &DismantleHero| {
                callback(ctx, &args.item)
            }),
        ))
    }
    fn remove_on_dismantle_hero(&self, callback: DismantleHeroCallbackId) {
        self.imp
            .remove_on_reducer::<DismantleHero>("dismantle_hero", callback.0)
    }
}
