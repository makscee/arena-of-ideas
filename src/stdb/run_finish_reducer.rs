// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct RunFinish {}

impl __sdk::spacetime_module::InModule for RunFinish {
    type Module = super::RemoteModule;
}

pub struct RunFinishCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `run_finish`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait run_finish {
    /// Request that the remote module invoke the reducer `run_finish` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_run_finish`] callbacks.
    fn run_finish(&self) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `run_finish`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`RunFinishCallbackId`] can be passed to [`Self::remove_on_run_finish`]
    /// to cancel the callback.
    fn on_run_finish(
        &self,
        callback: impl FnMut(&super::EventContext) + Send + 'static,
    ) -> RunFinishCallbackId;
    /// Cancel a callback previously registered by [`Self::on_run_finish`],
    /// causing it not to run in the future.
    fn remove_on_run_finish(&self, callback: RunFinishCallbackId);
}

impl run_finish for super::RemoteReducers {
    fn run_finish(&self) -> __anyhow::Result<()> {
        self.imp.call_reducer("run_finish", RunFinish {})
    }
    fn on_run_finish(
        &self,
        mut callback: impl FnMut(&super::EventContext) + Send + 'static,
    ) -> RunFinishCallbackId {
        RunFinishCallbackId(self.imp.on_reducer::<RunFinish>(
            "run_finish",
            Box::new(move |ctx: &super::EventContext, args: &RunFinish| callback(ctx)),
        ))
    }
    fn remove_on_run_finish(&self, callback: RunFinishCallbackId) {
        self.imp
            .remove_on_reducer::<RunFinish>("run_finish", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `run_finish`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_run_finish {
    /// Set the call-reducer flags for the reducer `run_finish` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn run_finish(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_run_finish for super::SetReducerFlags {
    fn run_finish(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("run_finish", flags);
    }
}
