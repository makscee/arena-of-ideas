// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_unit_name_type::TIncubatorUnitName;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_unit_name`.
///
/// Obtain a handle from the [`IncubatorUnitNameTableAccess::incubator_unit_name`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_unit_name()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit_name().on_insert(...)`.
pub struct IncubatorUnitNameTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorUnitName>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_unit_name`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorUnitNameTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorUnitNameTableHandle`], which mediates access to the table `incubator_unit_name`.
    fn incubator_unit_name(&self) -> IncubatorUnitNameTableHandle<'_>;
}

impl IncubatorUnitNameTableAccess for super::RemoteTables {
    fn incubator_unit_name(&self) -> IncubatorUnitNameTableHandle<'_> {
        IncubatorUnitNameTableHandle {
            imp: self
                .imp
                .get_table::<TIncubatorUnitName>("incubator_unit_name"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorUnitNameInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorUnitNameDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorUnitNameTableHandle<'ctx> {
    type Row = TIncubatorUnitName;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorUnitName> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorUnitNameInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitNameInsertCallbackId {
        IncubatorUnitNameInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorUnitNameInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorUnitNameDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitNameDeleteCallbackId {
        IncubatorUnitNameDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorUnitNameDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorUnitNameUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorUnitNameTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorUnitNameUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitNameUpdateCallbackId {
        IncubatorUnitNameUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorUnitNameUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorUnitName>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorUnitName| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_unit_name\"")
}

/// Access to the `id` unique index on the table `incubator_unit_name`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorUnitNameIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit_name().id().find(...)`.
pub struct IncubatorUnitNameIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorUnitName, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorUnitNameTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_unit_name`.
    pub fn id(&self) -> IncubatorUnitNameIdUnique<'ctx> {
        IncubatorUnitNameIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorUnitNameIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorUnitName> {
        self.imp.find(col_val)
    }
}
