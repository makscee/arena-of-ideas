// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused_imports)]
use spacetimedb_sdk::{
    anyhow::{anyhow, Result},
    identity::Identity,
    reducer::{Reducer, ReducerCallbackId, Status},
    sats::{de::Deserialize, ser::Serialize},
    spacetimedb_lib,
    table::{TableIter, TableType, TableWithPrimaryKey},
    Address,
};

#[derive(Serialize, Deserialize, Clone, PartialEq, Debug)]
pub struct THeroShard {
    pub owner: u64,
    pub hero: String,
    pub count: u32,
}

impl TableType for THeroShard {
    const TABLE_NAME: &'static str = "THeroShard";
    type ReducerEvent = super::ReducerEvent;
}

impl THeroShard {
    #[allow(unused)]
    pub fn filter_by_owner(owner: u64) -> TableIter<Self> {
        Self::filter(|row| row.owner == owner)
    }
    #[allow(unused)]
    pub fn filter_by_hero(hero: String) -> TableIter<Self> {
        Self::filter(|row| row.hero == hero)
    }
    #[allow(unused)]
    pub fn filter_by_count(count: u32) -> TableIter<Self> {
        Self::filter(|row| row.count == count)
    }
}
