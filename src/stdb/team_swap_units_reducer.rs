// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct TeamSwapUnits {
    pub team: u64,
    pub from: u8,
    pub to: u8,
}

impl __sdk::spacetime_module::InModule for TeamSwapUnits {
    type Module = super::RemoteModule;
}

pub struct TeamSwapUnitsCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `team_swap_units`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait team_swap_units {
    /// Request that the remote module invoke the reducer `team_swap_units` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_team_swap_units`] callbacks.
    fn team_swap_units(&self, team: u64, from: u8, to: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `team_swap_units`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`TeamSwapUnitsCallbackId`] can be passed to [`Self::remove_on_team_swap_units`]
    /// to cancel the callback.
    fn on_team_swap_units(
        &self,
        callback: impl FnMut(&super::EventContext, &u64, &u8, &u8) + Send + 'static,
    ) -> TeamSwapUnitsCallbackId;
    /// Cancel a callback previously registered by [`Self::on_team_swap_units`],
    /// causing it not to run in the future.
    fn remove_on_team_swap_units(&self, callback: TeamSwapUnitsCallbackId);
}

impl team_swap_units for super::RemoteReducers {
    fn team_swap_units(&self, team: u64, from: u8, to: u8) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("team_swap_units", TeamSwapUnits { team, from, to })
    }
    fn on_team_swap_units(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64, &u8, &u8) + Send + 'static,
    ) -> TeamSwapUnitsCallbackId {
        TeamSwapUnitsCallbackId(self.imp.on_reducer::<TeamSwapUnits>(
            "team_swap_units",
            Box::new(move |ctx: &super::EventContext, args: &TeamSwapUnits| {
                callback(ctx, &args.team, &args.from, &args.to)
            }),
        ))
    }
    fn remove_on_team_swap_units(&self, callback: TeamSwapUnitsCallbackId) {
        self.imp
            .remove_on_reducer::<TeamSwapUnits>("team_swap_units", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `team_swap_units`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_team_swap_units {
    /// Set the call-reducer flags for the reducer `team_swap_units` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn team_swap_units(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_team_swap_units for super::SetReducerFlags {
    fn team_swap_units(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("team_swap_units", flags);
    }
}
