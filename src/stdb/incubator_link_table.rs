// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_link_type::TIncubatorLink;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_link`.
///
/// Obtain a handle from the [`IncubatorLinkTableAccess::incubator_link`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_link()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_link().on_insert(...)`.
pub struct IncubatorLinkTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorLink>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_link`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorLinkTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorLinkTableHandle`], which mediates access to the table `incubator_link`.
    fn incubator_link(&self) -> IncubatorLinkTableHandle<'_>;
}

impl IncubatorLinkTableAccess for super::RemoteTables {
    fn incubator_link(&self) -> IncubatorLinkTableHandle<'_> {
        IncubatorLinkTableHandle {
            imp: self.imp.get_table::<TIncubatorLink>("incubator_link"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorLinkInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorLinkDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorLinkTableHandle<'ctx> {
    type Row = TIncubatorLink;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorLink> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorLinkInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorLinkInsertCallbackId {
        IncubatorLinkInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorLinkInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorLinkDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorLinkDeleteCallbackId {
        IncubatorLinkDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorLinkDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorLinkUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorLinkTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorLinkUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorLinkUpdateCallbackId {
        IncubatorLinkUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorLinkUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorLink>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<String>(
        raw_updates,
        |row: &TIncubatorLink| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_link\"")
}

/// Access to the `id` unique index on the table `incubator_link`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorLinkIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_link().id().find(...)`.
pub struct IncubatorLinkIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorLink, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorLinkTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_link`.
    pub fn id(&self) -> IncubatorLinkIdUnique<'ctx> {
        IncubatorLinkIdUnique {
            imp: self
                .imp
                .get_unique_constraint::<String>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorLinkIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TIncubatorLink> {
        self.imp.find(col_val)
    }
}
