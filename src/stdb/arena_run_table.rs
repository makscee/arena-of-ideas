// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::fusion_type::Fusion;
use super::game_mode_type::GameMode;
use super::reward_type::Reward;
use super::shop_slot_type::ShopSlot;
use super::t_arena_run_type::TArenaRun;
use super::team_slot_type::TeamSlot;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `arena_run`.
///
/// Obtain a handle from the [`ArenaRunTableAccess::arena_run`] method on [`super::RemoteTables`],
/// like `ctx.db.arena_run()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.arena_run().on_insert(...)`.
pub struct ArenaRunTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TArenaRun>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `arena_run`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ArenaRunTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ArenaRunTableHandle`], which mediates access to the table `arena_run`.
    fn arena_run(&self) -> ArenaRunTableHandle<'_>;
}

impl ArenaRunTableAccess for super::RemoteTables {
    fn arena_run(&self) -> ArenaRunTableHandle<'_> {
        ArenaRunTableHandle {
            imp: self.imp.get_table::<TArenaRun>("arena_run"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ArenaRunInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct ArenaRunDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for ArenaRunTableHandle<'ctx> {
    type Row = TArenaRun;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TArenaRun> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ArenaRunInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ArenaRunInsertCallbackId {
        ArenaRunInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ArenaRunInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ArenaRunDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ArenaRunDeleteCallbackId {
        ArenaRunDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ArenaRunDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ArenaRunUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for ArenaRunTableHandle<'ctx> {
    type UpdateCallbackId = ArenaRunUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ArenaRunUpdateCallbackId {
        ArenaRunUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ArenaRunUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TArenaRun>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TArenaRun| &row.id,
    )
    .context("Failed to parse table update for table \"arena_run\"")
}

/// Access to the `id` unique index on the table `arena_run`,
/// which allows point queries on the field of the same name
/// via the [`ArenaRunIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.arena_run().id().find(...)`.
pub struct ArenaRunIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TArenaRun, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ArenaRunTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `arena_run`.
    pub fn id(&self) -> ArenaRunIdUnique<'ctx> {
        ArenaRunIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ArenaRunIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TArenaRun> {
        self.imp.find(col_val)
    }
}

/// Access to the `owner` unique index on the table `arena_run`,
/// which allows point queries on the field of the same name
/// via the [`ArenaRunOwnerUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.arena_run().owner().find(...)`.
pub struct ArenaRunOwnerUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TArenaRun, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ArenaRunTableHandle<'ctx> {
    /// Get a handle on the `owner` unique index on the table `arena_run`.
    pub fn owner(&self) -> ArenaRunOwnerUnique<'ctx> {
        ArenaRunOwnerUnique {
            imp: self
                .imp
                .get_unique_constraint::<u64>("owner", |row| &row.owner),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ArenaRunOwnerUnique<'ctx> {
    /// Find the subscribed row whose `owner` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TArenaRun> {
        self.imp.find(col_val)
    }
}
