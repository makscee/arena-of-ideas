// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct TeamCreate {
    pub name: String,
}

impl __sdk::spacetime_module::InModule for TeamCreate {
    type Module = super::RemoteModule;
}

pub struct TeamCreateCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `team_create`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait team_create {
    /// Request that the remote module invoke the reducer `team_create` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_team_create`] callbacks.
    fn team_create(&self, name: String) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `team_create`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`TeamCreateCallbackId`] can be passed to [`Self::remove_on_team_create`]
    /// to cancel the callback.
    fn on_team_create(
        &self,
        callback: impl FnMut(&super::EventContext, &String) + Send + 'static,
    ) -> TeamCreateCallbackId;
    /// Cancel a callback previously registered by [`Self::on_team_create`],
    /// causing it not to run in the future.
    fn remove_on_team_create(&self, callback: TeamCreateCallbackId);
}

impl team_create for super::RemoteReducers {
    fn team_create(&self, name: String) -> __anyhow::Result<()> {
        self.imp.call_reducer("team_create", TeamCreate { name })
    }
    fn on_team_create(
        &self,
        mut callback: impl FnMut(&super::EventContext, &String) + Send + 'static,
    ) -> TeamCreateCallbackId {
        TeamCreateCallbackId(self.imp.on_reducer::<TeamCreate>(
            "team_create",
            Box::new(move |ctx: &super::EventContext, args: &TeamCreate| callback(ctx, &args.name)),
        ))
    }
    fn remove_on_team_create(&self, callback: TeamCreateCallbackId) {
        self.imp
            .remove_on_reducer::<TeamCreate>("team_create", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `team_create`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_team_create {
    /// Set the call-reducer flags for the reducer `team_create` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn team_create(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_team_create for super::SetReducerFlags {
    fn team_create(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("team_create", flags);
    }
}
