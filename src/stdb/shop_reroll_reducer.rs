// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct ShopReroll {}

impl __sdk::spacetime_module::InModule for ShopReroll {
    type Module = super::RemoteModule;
}

pub struct ShopRerollCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `shop_reroll`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait shop_reroll {
    /// Request that the remote module invoke the reducer `shop_reroll` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_shop_reroll`] callbacks.
    fn shop_reroll(&self) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `shop_reroll`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`ShopRerollCallbackId`] can be passed to [`Self::remove_on_shop_reroll`]
    /// to cancel the callback.
    fn on_shop_reroll(
        &self,
        callback: impl FnMut(&super::EventContext) + Send + 'static,
    ) -> ShopRerollCallbackId;
    /// Cancel a callback previously registered by [`Self::on_shop_reroll`],
    /// causing it not to run in the future.
    fn remove_on_shop_reroll(&self, callback: ShopRerollCallbackId);
}

impl shop_reroll for super::RemoteReducers {
    fn shop_reroll(&self) -> __anyhow::Result<()> {
        self.imp.call_reducer("shop_reroll", ShopReroll {})
    }
    fn on_shop_reroll(
        &self,
        mut callback: impl FnMut(&super::EventContext) + Send + 'static,
    ) -> ShopRerollCallbackId {
        ShopRerollCallbackId(self.imp.on_reducer::<ShopReroll>(
            "shop_reroll",
            Box::new(move |ctx: &super::EventContext, args: &ShopReroll| callback(ctx)),
        ))
    }
    fn remove_on_shop_reroll(&self, callback: ShopRerollCallbackId) {
        self.imp
            .remove_on_reducer::<ShopReroll>("shop_reroll", callback.0)
    }
}
