// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct ShopReorder {
    pub from: u8,
    pub to: u8,
}

impl __sdk::spacetime_module::InModule for ShopReorder {
    type Module = super::RemoteModule;
}

pub struct ShopReorderCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `shop_reorder`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait shop_reorder {
    /// Request that the remote module invoke the reducer `shop_reorder` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_shop_reorder`] callbacks.
    fn shop_reorder(&self, from: u8, to: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `shop_reorder`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`ShopReorderCallbackId`] can be passed to [`Self::remove_on_shop_reorder`]
    /// to cancel the callback.
    fn on_shop_reorder(
        &self,
        callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> ShopReorderCallbackId;
    /// Cancel a callback previously registered by [`Self::on_shop_reorder`],
    /// causing it not to run in the future.
    fn remove_on_shop_reorder(&self, callback: ShopReorderCallbackId);
}

impl shop_reorder for super::RemoteReducers {
    fn shop_reorder(&self, from: u8, to: u8) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("shop_reorder", ShopReorder { from, to })
    }
    fn on_shop_reorder(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> ShopReorderCallbackId {
        ShopReorderCallbackId(self.imp.on_reducer::<ShopReorder>(
            "shop_reorder",
            Box::new(move |ctx: &super::EventContext, args: &ShopReorder| {
                callback(ctx, &args.from, &args.to)
            }),
        ))
    }
    fn remove_on_shop_reorder(&self, callback: ShopReorderCallbackId) {
        self.imp
            .remove_on_reducer::<ShopReorder>("shop_reorder", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `shop_reorder`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_shop_reorder {
    /// Set the call-reducer flags for the reducer `shop_reorder` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn shop_reorder(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_shop_reorder for super::SetReducerFlags {
    fn shop_reorder(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("shop_reorder", flags);
    }
}
