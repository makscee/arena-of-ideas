// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

use super::t_battle_result_type::TBattleResult;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct SubmitBattleResult {
    pub result: TBattleResult,
}

impl __sdk::spacetime_module::InModule for SubmitBattleResult {
    type Module = super::RemoteModule;
}

pub struct SubmitBattleResultCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `submit_battle_result`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait submit_battle_result {
    /// Request that the remote module invoke the reducer `submit_battle_result` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_submit_battle_result`] callbacks.
    fn submit_battle_result(&self, result: TBattleResult) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `submit_battle_result`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`SubmitBattleResultCallbackId`] can be passed to [`Self::remove_on_submit_battle_result`]
    /// to cancel the callback.
    fn on_submit_battle_result(
        &self,
        callback: impl FnMut(&super::EventContext, &TBattleResult) + Send + 'static,
    ) -> SubmitBattleResultCallbackId;
    /// Cancel a callback previously registered by [`Self::on_submit_battle_result`],
    /// causing it not to run in the future.
    fn remove_on_submit_battle_result(&self, callback: SubmitBattleResultCallbackId);
}

impl submit_battle_result for super::RemoteReducers {
    fn submit_battle_result(&self, result: TBattleResult) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("submit_battle_result", SubmitBattleResult { result })
    }
    fn on_submit_battle_result(
        &self,
        mut callback: impl FnMut(&super::EventContext, &TBattleResult) + Send + 'static,
    ) -> SubmitBattleResultCallbackId {
        SubmitBattleResultCallbackId(self.imp.on_reducer::<SubmitBattleResult>(
            "submit_battle_result",
            Box::new(
                move |ctx: &super::EventContext, args: &SubmitBattleResult| {
                    callback(ctx, &args.result)
                },
            ),
        ))
    }
    fn remove_on_submit_battle_result(&self, callback: SubmitBattleResultCallbackId) {
        self.imp
            .remove_on_reducer::<SubmitBattleResult>("submit_battle_result", callback.0)
    }
}
