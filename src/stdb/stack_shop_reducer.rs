// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct StackShop {
    pub source: u8,
    pub target: u8,
}

impl __sdk::spacetime_module::InModule for StackShop {
    type Module = super::RemoteModule;
}

pub struct StackShopCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stack_shop`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stack_shop {
    /// Request that the remote module invoke the reducer `stack_shop` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_stack_shop`] callbacks.
    fn stack_shop(&self, source: u8, target: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `stack_shop`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`StackShopCallbackId`] can be passed to [`Self::remove_on_stack_shop`]
    /// to cancel the callback.
    fn on_stack_shop(
        &self,
        callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> StackShopCallbackId;
    /// Cancel a callback previously registered by [`Self::on_stack_shop`],
    /// causing it not to run in the future.
    fn remove_on_stack_shop(&self, callback: StackShopCallbackId);
}

impl stack_shop for super::RemoteReducers {
    fn stack_shop(&self, source: u8, target: u8) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("stack_shop", StackShop { source, target })
    }
    fn on_stack_shop(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> StackShopCallbackId {
        StackShopCallbackId(self.imp.on_reducer::<StackShop>(
            "stack_shop",
            Box::new(move |ctx: &super::EventContext, args: &StackShop| {
                callback(ctx, &args.source, &args.target)
            }),
        ))
    }
    fn remove_on_stack_shop(&self, callback: StackShopCallbackId) {
        self.imp
            .remove_on_reducer::<StackShop>("stack_shop", callback.0)
    }
}
