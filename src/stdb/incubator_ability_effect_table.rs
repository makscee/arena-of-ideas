// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_ability_effect_type::TIncubatorAbilityEffect;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_ability_effect`.
///
/// Obtain a handle from the [`IncubatorAbilityEffectTableAccess::incubator_ability_effect`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_ability_effect()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_ability_effect().on_insert(...)`.
pub struct IncubatorAbilityEffectTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorAbilityEffect>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_ability_effect`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorAbilityEffectTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorAbilityEffectTableHandle`], which mediates access to the table `incubator_ability_effect`.
    fn incubator_ability_effect(&self) -> IncubatorAbilityEffectTableHandle<'_>;
}

impl IncubatorAbilityEffectTableAccess for super::RemoteTables {
    fn incubator_ability_effect(&self) -> IncubatorAbilityEffectTableHandle<'_> {
        IncubatorAbilityEffectTableHandle {
            imp: self
                .imp
                .get_table::<TIncubatorAbilityEffect>("incubator_ability_effect"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorAbilityEffectInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorAbilityEffectDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorAbilityEffectTableHandle<'ctx> {
    type Row = TIncubatorAbilityEffect;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorAbilityEffect> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorAbilityEffectInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorAbilityEffectInsertCallbackId {
        IncubatorAbilityEffectInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorAbilityEffectInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorAbilityEffectDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorAbilityEffectDeleteCallbackId {
        IncubatorAbilityEffectDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorAbilityEffectDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorAbilityEffectUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorAbilityEffectTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorAbilityEffectUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorAbilityEffectUpdateCallbackId {
        IncubatorAbilityEffectUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorAbilityEffectUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorAbilityEffect>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorAbilityEffect| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_ability_effect\"")
}

/// Access to the `id` unique index on the table `incubator_ability_effect`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorAbilityEffectIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_ability_effect().id().find(...)`.
pub struct IncubatorAbilityEffectIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorAbilityEffect, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorAbilityEffectTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_ability_effect`.
    pub fn id(&self) -> IncubatorAbilityEffectIdUnique<'ctx> {
        IncubatorAbilityEffectIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorAbilityEffectIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorAbilityEffect> {
        self.imp.find(col_val)
    }
}
