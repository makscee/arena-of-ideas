// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchMoveOwnedUnitArgs {
    pub source_unit_id: u64,
    pub target_fusion_slot: i32,
    pub target_position: i32,
}

impl From<MatchMoveOwnedUnitArgs> for super::Reducer {
    fn from(args: MatchMoveOwnedUnitArgs) -> Self {
        Self::MatchMoveOwnedUnit {
            source_unit_id: args.source_unit_id,
            target_fusion_slot: args.target_fusion_slot,
            target_position: args.target_position,
        }
    }
}

impl __sdk::InModule for MatchMoveOwnedUnitArgs {
    type Module = super::RemoteModule;
}

pub struct MatchMoveOwnedUnitCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_move_owned_unit`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_move_owned_unit {
    /// Request that the remote module invoke the reducer `match_move_owned_unit` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_match_move_owned_unit`] callbacks.
    fn match_move_owned_unit(
        &self,
        source_unit_id: u64,
        target_fusion_slot: i32,
        target_position: i32,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `match_move_owned_unit`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`MatchMoveOwnedUnitCallbackId`] can be passed to [`Self::remove_on_match_move_owned_unit`]
    /// to cancel the callback.
    fn on_match_move_owned_unit(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &i32, &i32) + Send + 'static,
    ) -> MatchMoveOwnedUnitCallbackId;
    /// Cancel a callback previously registered by [`Self::on_match_move_owned_unit`],
    /// causing it not to run in the future.
    fn remove_on_match_move_owned_unit(&self, callback: MatchMoveOwnedUnitCallbackId);
}

impl match_move_owned_unit for super::RemoteReducers {
    fn match_move_owned_unit(
        &self,
        source_unit_id: u64,
        target_fusion_slot: i32,
        target_position: i32,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "match_move_owned_unit",
            MatchMoveOwnedUnitArgs {
                source_unit_id,
                target_fusion_slot,
                target_position,
            },
        )
    }
    fn on_match_move_owned_unit(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &i32, &i32) + Send + 'static,
    ) -> MatchMoveOwnedUnitCallbackId {
        MatchMoveOwnedUnitCallbackId(self.imp.on_reducer(
            "match_move_owned_unit",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::MatchMoveOwnedUnit {
                                    source_unit_id,
                                    target_fusion_slot,
                                    target_position,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, source_unit_id, target_fusion_slot, target_position)
            }),
        ))
    }
    fn remove_on_match_move_owned_unit(&self, callback: MatchMoveOwnedUnitCallbackId) {
        self.imp
            .remove_on_reducer("match_move_owned_unit", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `match_move_owned_unit`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_match_move_owned_unit {
    /// Set the call-reducer flags for the reducer `match_move_owned_unit` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn match_move_owned_unit(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_match_move_owned_unit for super::SetReducerFlags {
    fn match_move_owned_unit(&self, flags: __ws::CallReducerFlags) {
        self.imp
            .set_call_reducer_flags("match_move_owned_unit", flags);
    }
}
