// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct NodeSpawn {
    pub kind: String,
    pub data: String,
}

impl __sdk::spacetime_module::InModule for NodeSpawn {
    type Module = super::RemoteModule;
}

pub struct NodeSpawnCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `node_spawn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait node_spawn {
    /// Request that the remote module invoke the reducer `node_spawn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_node_spawn`] callbacks.
    fn node_spawn(&self, kind: String, data: String) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `node_spawn`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`NodeSpawnCallbackId`] can be passed to [`Self::remove_on_node_spawn`]
    /// to cancel the callback.
    fn on_node_spawn(
        &self,
        callback: impl FnMut(&super::EventContext, &String, &String) + Send + 'static,
    ) -> NodeSpawnCallbackId;
    /// Cancel a callback previously registered by [`Self::on_node_spawn`],
    /// causing it not to run in the future.
    fn remove_on_node_spawn(&self, callback: NodeSpawnCallbackId);
}

impl node_spawn for super::RemoteReducers {
    fn node_spawn(&self, kind: String, data: String) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("node_spawn", NodeSpawn { kind, data })
    }
    fn on_node_spawn(
        &self,
        mut callback: impl FnMut(&super::EventContext, &String, &String) + Send + 'static,
    ) -> NodeSpawnCallbackId {
        NodeSpawnCallbackId(self.imp.on_reducer::<NodeSpawn>(
            "node_spawn",
            Box::new(move |ctx: &super::EventContext, args: &NodeSpawn| {
                callback(ctx, &args.kind, &args.data)
            }),
        ))
    }
    fn remove_on_node_spawn(&self, callback: NodeSpawnCallbackId) {
        self.imp
            .remove_on_reducer::<NodeSpawn>("node_spawn", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `node_spawn`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_node_spawn {
    /// Set the call-reducer flags for the reducer `node_spawn` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn node_spawn(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_node_spawn for super::SetReducerFlags {
    fn node_spawn(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("node_spawn", flags);
    }
}
