// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused_imports)]
use spacetimedb_sdk::{
    anyhow::{anyhow, Result},
    identity::Identity,
    reducer::{Reducer, ReducerCallbackId, Status},
    sats::{de::Deserialize, ser::Serialize},
    spacetimedb_lib,
    table::{TableIter, TableType, TableWithPrimaryKey},
    Address, ScheduleAt,
};

#[derive(Serialize, Deserialize, Clone, PartialEq, Debug)]
pub struct TAbility {
    pub name: String,
    pub description: String,
    pub effect: String,
}

impl TableType for TAbility {
    const TABLE_NAME: &'static str = "TAbility";
    type ReducerEvent = super::ReducerEvent;
}

impl TableWithPrimaryKey for TAbility {
    type PrimaryKey = String;
    fn primary_key(&self) -> &Self::PrimaryKey {
        &self.name
    }
}

impl TAbility {
    #[allow(unused)]
    pub fn filter_by_name(name: String) -> TableIter<Self> {
        Self::filter(|row| row.name == name)
    }
    #[allow(unused)]
    pub fn find_by_name(name: String) -> Option<Self> {
        Self::find(|row| row.name == name)
    }
    #[allow(unused)]
    pub fn filter_by_description(description: String) -> TableIter<Self> {
        Self::filter(|row| row.description == description)
    }
    #[allow(unused)]
    pub fn filter_by_effect(effect: String) -> TableIter<Self> {
        Self::filter(|row| row.effect == effect)
    }
}
