// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_content_link_type::TContentLink;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `content_link`.
///
/// Obtain a handle from the [`ContentLinkTableAccess::content_link`] method on [`super::RemoteTables`],
/// like `ctx.db.content_link()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_link().on_insert(...)`.
pub struct ContentLinkTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TContentLink>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `content_link`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ContentLinkTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ContentLinkTableHandle`], which mediates access to the table `content_link`.
    fn content_link(&self) -> ContentLinkTableHandle<'_>;
}

impl ContentLinkTableAccess for super::RemoteTables {
    fn content_link(&self) -> ContentLinkTableHandle<'_> {
        ContentLinkTableHandle {
            imp: self.imp.get_table::<TContentLink>("content_link"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ContentLinkInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct ContentLinkDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for ContentLinkTableHandle<'ctx> {
    type Row = TContentLink;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TContentLink> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ContentLinkInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentLinkInsertCallbackId {
        ContentLinkInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ContentLinkInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ContentLinkDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentLinkDeleteCallbackId {
        ContentLinkDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ContentLinkDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ContentLinkUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for ContentLinkTableHandle<'ctx> {
    type UpdateCallbackId = ContentLinkUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ContentLinkUpdateCallbackId {
        ContentLinkUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ContentLinkUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TContentLink>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<String>(
        raw_updates,
        |row: &TContentLink| &row.from_to,
    )
    .context("Failed to parse table update for table \"content_link\"")
}

/// Access to the `from_to` unique index on the table `content_link`,
/// which allows point queries on the field of the same name
/// via the [`ContentLinkFromToUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_link().from_to().find(...)`.
pub struct ContentLinkFromToUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TContentLink, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ContentLinkTableHandle<'ctx> {
    /// Get a handle on the `from_to` unique index on the table `content_link`.
    pub fn from_to(&self) -> ContentLinkFromToUnique<'ctx> {
        ContentLinkFromToUnique {
            imp: self
                .imp
                .get_unique_constraint::<String>("from_to", |row| &row.from_to),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ContentLinkFromToUnique<'ctx> {
    /// Find the subscribed row whose `from_to` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TContentLink> {
        self.imp.find(col_val)
    }
}
