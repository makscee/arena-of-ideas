// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::t_node_type::TNode;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct IncubatorPushArgs {
    pub kind: String,
    pub nodes: Vec<TNode>,
}

impl From<IncubatorPushArgs> for super::Reducer {
    fn from(args: IncubatorPushArgs) -> Self {
        Self::IncubatorPush {
            kind: args.kind,
            nodes: args.nodes,
        }
    }
}

impl __sdk::InModule for IncubatorPushArgs {
    type Module = super::RemoteModule;
}

pub struct IncubatorPushCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `incubator_push`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait incubator_push {
    /// Request that the remote module invoke the reducer `incubator_push` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_incubator_push`] callbacks.
    fn incubator_push(&self, kind: String, nodes: Vec<TNode>) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `incubator_push`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`IncubatorPushCallbackId`] can be passed to [`Self::remove_on_incubator_push`]
    /// to cancel the callback.
    fn on_incubator_push(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &String, &Vec<TNode>) + Send + 'static,
    ) -> IncubatorPushCallbackId;
    /// Cancel a callback previously registered by [`Self::on_incubator_push`],
    /// causing it not to run in the future.
    fn remove_on_incubator_push(&self, callback: IncubatorPushCallbackId);
}

impl incubator_push for super::RemoteReducers {
    fn incubator_push(&self, kind: String, nodes: Vec<TNode>) -> __sdk::Result<()> {
        self.imp
            .call_reducer("incubator_push", IncubatorPushArgs { kind, nodes })
    }
    fn on_incubator_push(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &String, &Vec<TNode>) + Send + 'static,
    ) -> IncubatorPushCallbackId {
        IncubatorPushCallbackId(self.imp.on_reducer(
            "incubator_push",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::IncubatorPush { kind, nodes },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, kind, nodes)
            }),
        ))
    }
    fn remove_on_incubator_push(&self, callback: IncubatorPushCallbackId) {
        self.imp.remove_on_reducer("incubator_push", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `incubator_push`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_incubator_push {
    /// Set the call-reducer flags for the reducer `incubator_push` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn incubator_push(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_incubator_push for super::SetReducerFlags {
    fn incubator_push(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("incubator_push", flags);
    }
}
