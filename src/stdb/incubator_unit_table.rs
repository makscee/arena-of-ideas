// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_unit_type::TIncubatorUnit;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_unit`.
///
/// Obtain a handle from the [`IncubatorUnitTableAccess::incubator_unit`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_unit()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit().on_insert(...)`.
pub struct IncubatorUnitTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorUnit>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_unit`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorUnitTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorUnitTableHandle`], which mediates access to the table `incubator_unit`.
    fn incubator_unit(&self) -> IncubatorUnitTableHandle<'_>;
}

impl IncubatorUnitTableAccess for super::RemoteTables {
    fn incubator_unit(&self) -> IncubatorUnitTableHandle<'_> {
        IncubatorUnitTableHandle {
            imp: self.imp.get_table::<TIncubatorUnit>("incubator_unit"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorUnitInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorUnitDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorUnitTableHandle<'ctx> {
    type Row = TIncubatorUnit;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorUnit> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorUnitInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitInsertCallbackId {
        IncubatorUnitInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorUnitInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorUnitDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitDeleteCallbackId {
        IncubatorUnitDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorUnitDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorUnitUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorUnitTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorUnitUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorUnitUpdateCallbackId {
        IncubatorUnitUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorUnitUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorUnit>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorUnit| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_unit\"")
}

/// Access to the `id` unique index on the table `incubator_unit`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorUnitIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_unit().id().find(...)`.
pub struct IncubatorUnitIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorUnit, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorUnitTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_unit`.
    pub fn id(&self) -> IncubatorUnitIdUnique<'ctx> {
        IncubatorUnitIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorUnitIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorUnit> {
        self.imp.find(col_val)
    }
}
