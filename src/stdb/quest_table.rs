// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::game_mode_type::GameMode;
use super::quest_variant_type::QuestVariant;
use super::t_quest_type::TQuest;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `quest`.
///
/// Obtain a handle from the [`QuestTableAccess::quest`] method on [`super::RemoteTables`],
/// like `ctx.db.quest()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quest().on_insert(...)`.
pub struct QuestTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TQuest>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `quest`.
///
/// Implemented for [`super::RemoteTables`].
pub trait QuestTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`QuestTableHandle`], which mediates access to the table `quest`.
    fn quest(&self) -> QuestTableHandle<'_>;
}

impl QuestTableAccess for super::RemoteTables {
    fn quest(&self) -> QuestTableHandle<'_> {
        QuestTableHandle {
            imp: self.imp.get_table::<TQuest>("quest"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct QuestInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct QuestDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for QuestTableHandle<'ctx> {
    type Row = TQuest;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TQuest> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = QuestInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QuestInsertCallbackId {
        QuestInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: QuestInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = QuestDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QuestDeleteCallbackId {
        QuestDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: QuestDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct QuestUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for QuestTableHandle<'ctx> {
    type UpdateCallbackId = QuestUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> QuestUpdateCallbackId {
        QuestUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: QuestUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TQuest>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TQuest| &row.id,
    )
    .context("Failed to parse table update for table \"quest\"")
}

/// Access to the `id` unique index on the table `quest`,
/// which allows point queries on the field of the same name
/// via the [`QuestIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quest().id().find(...)`.
pub struct QuestIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TQuest, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> QuestTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `quest`.
    pub fn id(&self) -> QuestIdUnique<'ctx> {
        QuestIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> QuestIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TQuest> {
        self.imp.find(col_val)
    }
}
