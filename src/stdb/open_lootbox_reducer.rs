// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct OpenLootbox {
    pub id: u64,
}

impl __sdk::spacetime_module::InModule for OpenLootbox {
    type Module = super::RemoteModule;
}

pub struct OpenLootboxCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `open_lootbox`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait open_lootbox {
    /// Request that the remote module invoke the reducer `open_lootbox` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_open_lootbox`] callbacks.
    fn open_lootbox(&self, id: u64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `open_lootbox`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`OpenLootboxCallbackId`] can be passed to [`Self::remove_on_open_lootbox`]
    /// to cancel the callback.
    fn on_open_lootbox(
        &self,
        callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> OpenLootboxCallbackId;
    /// Cancel a callback previously registered by [`Self::on_open_lootbox`],
    /// causing it not to run in the future.
    fn remove_on_open_lootbox(&self, callback: OpenLootboxCallbackId);
}

impl open_lootbox for super::RemoteReducers {
    fn open_lootbox(&self, id: u64) -> __anyhow::Result<()> {
        self.imp.call_reducer("open_lootbox", OpenLootbox { id })
    }
    fn on_open_lootbox(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> OpenLootboxCallbackId {
        OpenLootboxCallbackId(self.imp.on_reducer::<OpenLootbox>(
            "open_lootbox",
            Box::new(move |ctx: &super::EventContext, args: &OpenLootbox| callback(ctx, &args.id)),
        ))
    }
    fn remove_on_open_lootbox(&self, callback: OpenLootboxCallbackId) {
        self.imp
            .remove_on_reducer::<OpenLootbox>("open_lootbox", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `open_lootbox`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_open_lootbox {
    /// Set the call-reducer flags for the reducer `open_lootbox` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn open_lootbox(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_open_lootbox for super::SetReducerFlags {
    fn open_lootbox(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("open_lootbox", flags);
    }
}
