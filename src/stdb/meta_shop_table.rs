// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::item_kind_type::ItemKind;
use super::t_meta_shop_type::TMetaShop;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `meta_shop`.
///
/// Obtain a handle from the [`MetaShopTableAccess::meta_shop`] method on [`super::RemoteTables`],
/// like `ctx.db.meta_shop()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.meta_shop().on_insert(...)`.
pub struct MetaShopTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TMetaShop>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `meta_shop`.
///
/// Implemented for [`super::RemoteTables`].
pub trait MetaShopTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`MetaShopTableHandle`], which mediates access to the table `meta_shop`.
    fn meta_shop(&self) -> MetaShopTableHandle<'_>;
}

impl MetaShopTableAccess for super::RemoteTables {
    fn meta_shop(&self) -> MetaShopTableHandle<'_> {
        MetaShopTableHandle {
            imp: self.imp.get_table::<TMetaShop>("meta_shop"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct MetaShopInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct MetaShopDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for MetaShopTableHandle<'ctx> {
    type Row = TMetaShop;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TMetaShop> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = MetaShopInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> MetaShopInsertCallbackId {
        MetaShopInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: MetaShopInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = MetaShopDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> MetaShopDeleteCallbackId {
        MetaShopDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: MetaShopDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct MetaShopUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for MetaShopTableHandle<'ctx> {
    type UpdateCallbackId = MetaShopUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> MetaShopUpdateCallbackId {
        MetaShopUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: MetaShopUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TMetaShop>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TMetaShop| &row.id,
    )
    .context("Failed to parse table update for table \"meta_shop\"")
}

/// Access to the `id` unique index on the table `meta_shop`,
/// which allows point queries on the field of the same name
/// via the [`MetaShopIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.meta_shop().id().find(...)`.
pub struct MetaShopIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TMetaShop, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> MetaShopTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `meta_shop`.
    pub fn id(&self) -> MetaShopIdUnique<'ctx> {
        MetaShopIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> MetaShopIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TMetaShop> {
        self.imp.find(col_val)
    }
}
