// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_status_trigger_type::TIncubatorStatusTrigger;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_status_trigger`.
///
/// Obtain a handle from the [`IncubatorStatusTriggerTableAccess::incubator_status_trigger`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_status_trigger()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_status_trigger().on_insert(...)`.
pub struct IncubatorStatusTriggerTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorStatusTrigger>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_status_trigger`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorStatusTriggerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorStatusTriggerTableHandle`], which mediates access to the table `incubator_status_trigger`.
    fn incubator_status_trigger(&self) -> IncubatorStatusTriggerTableHandle<'_>;
}

impl IncubatorStatusTriggerTableAccess for super::RemoteTables {
    fn incubator_status_trigger(&self) -> IncubatorStatusTriggerTableHandle<'_> {
        IncubatorStatusTriggerTableHandle {
            imp: self
                .imp
                .get_table::<TIncubatorStatusTrigger>("incubator_status_trigger"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorStatusTriggerInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorStatusTriggerDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorStatusTriggerTableHandle<'ctx> {
    type Row = TIncubatorStatusTrigger;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorStatusTrigger> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorStatusTriggerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusTriggerInsertCallbackId {
        IncubatorStatusTriggerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorStatusTriggerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorStatusTriggerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusTriggerDeleteCallbackId {
        IncubatorStatusTriggerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorStatusTriggerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorStatusTriggerUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorStatusTriggerTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorStatusTriggerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusTriggerUpdateCallbackId {
        IncubatorStatusTriggerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorStatusTriggerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorStatusTrigger>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorStatusTrigger| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_status_trigger\"")
}

/// Access to the `id` unique index on the table `incubator_status_trigger`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorStatusTriggerIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_status_trigger().id().find(...)`.
pub struct IncubatorStatusTriggerIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorStatusTrigger, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorStatusTriggerTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_status_trigger`.
    pub fn id(&self) -> IncubatorStatusTriggerIdUnique<'ctx> {
        IncubatorStatusTriggerIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorStatusTriggerIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorStatusTrigger> {
        self.imp.find(col_val)
    }
}
