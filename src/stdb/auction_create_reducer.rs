// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct AuctionCreate {
    pub item_id: u64,
    pub count: u32,
    pub price: i64,
}

impl __sdk::spacetime_module::InModule for AuctionCreate {
    type Module = super::RemoteModule;
}

pub struct AuctionCreateCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `auction_create`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait auction_create {
    /// Request that the remote module invoke the reducer `auction_create` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_auction_create`] callbacks.
    fn auction_create(&self, item_id: u64, count: u32, price: i64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `auction_create`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`AuctionCreateCallbackId`] can be passed to [`Self::remove_on_auction_create`]
    /// to cancel the callback.
    fn on_auction_create(
        &self,
        callback: impl FnMut(&super::EventContext, &u64, &u32, &i64) + Send + 'static,
    ) -> AuctionCreateCallbackId;
    /// Cancel a callback previously registered by [`Self::on_auction_create`],
    /// causing it not to run in the future.
    fn remove_on_auction_create(&self, callback: AuctionCreateCallbackId);
}

impl auction_create for super::RemoteReducers {
    fn auction_create(&self, item_id: u64, count: u32, price: i64) -> __anyhow::Result<()> {
        self.imp.call_reducer(
            "auction_create",
            AuctionCreate {
                item_id,
                count,
                price,
            },
        )
    }
    fn on_auction_create(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64, &u32, &i64) + Send + 'static,
    ) -> AuctionCreateCallbackId {
        AuctionCreateCallbackId(self.imp.on_reducer::<AuctionCreate>(
            "auction_create",
            Box::new(move |ctx: &super::EventContext, args: &AuctionCreate| {
                callback(ctx, &args.item_id, &args.count, &args.price)
            }),
        ))
    }
    fn remove_on_auction_create(&self, callback: AuctionCreateCallbackId) {
        self.imp
            .remove_on_reducer::<AuctionCreate>("auction_create", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `auction_create`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_auction_create {
    /// Set the call-reducer flags for the reducer `auction_create` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn auction_create(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_auction_create for super::SetReducerFlags {
    fn auction_create(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("auction_create", flags);
    }
}
