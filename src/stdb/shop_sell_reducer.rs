// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct ShopSell {
    pub slot: u8,
}

impl __sdk::spacetime_module::InModule for ShopSell {
    type Module = super::RemoteModule;
}

pub struct ShopSellCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `shop_sell`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait shop_sell {
    /// Request that the remote module invoke the reducer `shop_sell` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_shop_sell`] callbacks.
    fn shop_sell(&self, slot: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `shop_sell`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`ShopSellCallbackId`] can be passed to [`Self::remove_on_shop_sell`]
    /// to cancel the callback.
    fn on_shop_sell(
        &self,
        callback: impl FnMut(&super::EventContext, &u8) + Send + 'static,
    ) -> ShopSellCallbackId;
    /// Cancel a callback previously registered by [`Self::on_shop_sell`],
    /// causing it not to run in the future.
    fn remove_on_shop_sell(&self, callback: ShopSellCallbackId);
}

impl shop_sell for super::RemoteReducers {
    fn shop_sell(&self, slot: u8) -> __anyhow::Result<()> {
        self.imp.call_reducer("shop_sell", ShopSell { slot })
    }
    fn on_shop_sell(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8) + Send + 'static,
    ) -> ShopSellCallbackId {
        ShopSellCallbackId(self.imp.on_reducer::<ShopSell>(
            "shop_sell",
            Box::new(move |ctx: &super::EventContext, args: &ShopSell| callback(ctx, &args.slot)),
        ))
    }
    fn remove_on_shop_sell(&self, callback: ShopSellCallbackId) {
        self.imp
            .remove_on_reducer::<ShopSell>("shop_sell", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `shop_sell`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_shop_sell {
    /// Set the call-reducer flags for the reducer `shop_sell` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn shop_sell(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_shop_sell for super::SetReducerFlags {
    fn shop_sell(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("shop_sell", flags);
    }
}
