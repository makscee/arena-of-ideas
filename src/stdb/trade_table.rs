// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::item_bundle_type::ItemBundle;
use super::t_trade_type::TTrade;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `trade`.
///
/// Obtain a handle from the [`TradeTableAccess::trade`] method on [`super::RemoteTables`],
/// like `ctx.db.trade()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.trade().on_insert(...)`.
pub struct TradeTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TTrade>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `trade`.
///
/// Implemented for [`super::RemoteTables`].
pub trait TradeTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`TradeTableHandle`], which mediates access to the table `trade`.
    fn trade(&self) -> TradeTableHandle<'_>;
}

impl TradeTableAccess for super::RemoteTables {
    fn trade(&self) -> TradeTableHandle<'_> {
        TradeTableHandle {
            imp: self.imp.get_table::<TTrade>("trade"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct TradeInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct TradeDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for TradeTableHandle<'ctx> {
    type Row = TTrade;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TTrade> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = TradeInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TradeInsertCallbackId {
        TradeInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: TradeInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = TradeDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TradeDeleteCallbackId {
        TradeDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: TradeDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct TradeUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for TradeTableHandle<'ctx> {
    type UpdateCallbackId = TradeUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> TradeUpdateCallbackId {
        TradeUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: TradeUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TTrade>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TTrade| &row.id,
    )
    .context("Failed to parse table update for table \"trade\"")
}

/// Access to the `id` unique index on the table `trade`,
/// which allows point queries on the field of the same name
/// via the [`TradeIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.trade().id().find(...)`.
pub struct TradeIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TTrade, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TradeTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `trade`.
    pub fn id(&self) -> TradeIdUnique<'ctx> {
        TradeIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TradeIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TTrade> {
        self.imp.find(col_val)
    }
}
