// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused_imports)]
use spacetimedb_sdk::{
    anyhow::{anyhow, Result},
    identity::Identity,
    reducer::{Reducer, ReducerCallbackId, Status},
    sats::{de::Deserialize, ser::Serialize},
    spacetimedb_lib,
    table::{TableIter, TableType, TableWithPrimaryKey},
    Address, ScheduleAt,
};

#[derive(Serialize, Deserialize, Clone, PartialEq, Debug)]
pub struct TIncubatorVote {
    pub id: u64,
    pub owner: u64,
    pub target: u64,
    pub vote: bool,
}

impl TableType for TIncubatorVote {
    const TABLE_NAME: &'static str = "TIncubatorVote";
    type ReducerEvent = super::ReducerEvent;
}

impl TableWithPrimaryKey for TIncubatorVote {
    type PrimaryKey = u64;
    fn primary_key(&self) -> &Self::PrimaryKey {
        &self.id
    }
}

impl TIncubatorVote {
    #[allow(unused)]
    pub fn filter_by_id(id: u64) -> TableIter<Self> {
        Self::filter(|row| row.id == id)
    }
    #[allow(unused)]
    pub fn find_by_id(id: u64) -> Option<Self> {
        Self::find(|row| row.id == id)
    }
    #[allow(unused)]
    pub fn filter_by_owner(owner: u64) -> TableIter<Self> {
        Self::filter(|row| row.owner == owner)
    }
    #[allow(unused)]
    pub fn filter_by_target(target: u64) -> TableIter<Self> {
        Self::filter(|row| row.target == target)
    }
    #[allow(unused)]
    pub fn filter_by_vote(vote: bool) -> TableIter<Self> {
        Self::filter(|row| row.vote == vote)
    }
}
