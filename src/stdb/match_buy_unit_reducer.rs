// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchBuyUnitArgs {
    pub shop_idx: u8,
    pub slot: u8,
}

impl From<MatchBuyUnitArgs> for super::Reducer {
    fn from(args: MatchBuyUnitArgs) -> Self {
        Self::MatchBuyUnit {
            shop_idx: args.shop_idx,
            slot: args.slot,
        }
    }
}

impl __sdk::InModule for MatchBuyUnitArgs {
    type Module = super::RemoteModule;
}

pub struct MatchBuyUnitCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_buy_unit`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_buy_unit {
    /// Request that the remote module invoke the reducer `match_buy_unit` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_match_buy_unit`] callbacks.
    fn match_buy_unit(&self, shop_idx: u8, slot: u8) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `match_buy_unit`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`MatchBuyUnitCallbackId`] can be passed to [`Self::remove_on_match_buy_unit`]
    /// to cancel the callback.
    fn on_match_buy_unit(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u8, &u8) + Send + 'static,
    ) -> MatchBuyUnitCallbackId;
    /// Cancel a callback previously registered by [`Self::on_match_buy_unit`],
    /// causing it not to run in the future.
    fn remove_on_match_buy_unit(&self, callback: MatchBuyUnitCallbackId);
}

impl match_buy_unit for super::RemoteReducers {
    fn match_buy_unit(&self, shop_idx: u8, slot: u8) -> __sdk::Result<()> {
        self.imp
            .call_reducer("match_buy_unit", MatchBuyUnitArgs { shop_idx, slot })
    }
    fn on_match_buy_unit(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u8, &u8) + Send + 'static,
    ) -> MatchBuyUnitCallbackId {
        MatchBuyUnitCallbackId(self.imp.on_reducer(
            "match_buy_unit",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::MatchBuyUnit { shop_idx, slot },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, shop_idx, slot)
            }),
        ))
    }
    fn remove_on_match_buy_unit(&self, callback: MatchBuyUnitCallbackId) {
        self.imp.remove_on_reducer("match_buy_unit", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `match_buy_unit`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_match_buy_unit {
    /// Set the call-reducer flags for the reducer `match_buy_unit` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn match_buy_unit(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_match_buy_unit for super::SetReducerFlags {
    fn match_buy_unit(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("match_buy_unit", flags);
    }
}
