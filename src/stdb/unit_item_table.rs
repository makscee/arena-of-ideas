// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::fused_unit_type::FusedUnit;
use super::t_unit_item_type::TUnitItem;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `unit_item`.
///
/// Obtain a handle from the [`UnitItemTableAccess::unit_item`] method on [`super::RemoteTables`],
/// like `ctx.db.unit_item()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.unit_item().on_insert(...)`.
pub struct UnitItemTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TUnitItem>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `unit_item`.
///
/// Implemented for [`super::RemoteTables`].
pub trait UnitItemTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`UnitItemTableHandle`], which mediates access to the table `unit_item`.
    fn unit_item(&self) -> UnitItemTableHandle<'_>;
}

impl UnitItemTableAccess for super::RemoteTables {
    fn unit_item(&self) -> UnitItemTableHandle<'_> {
        UnitItemTableHandle {
            imp: self.imp.get_table::<TUnitItem>("unit_item"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct UnitItemInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct UnitItemDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for UnitItemTableHandle<'ctx> {
    type Row = TUnitItem;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TUnitItem> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = UnitItemInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> UnitItemInsertCallbackId {
        UnitItemInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: UnitItemInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = UnitItemDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> UnitItemDeleteCallbackId {
        UnitItemDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: UnitItemDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct UnitItemUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for UnitItemTableHandle<'ctx> {
    type UpdateCallbackId = UnitItemUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> UnitItemUpdateCallbackId {
        UnitItemUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: UnitItemUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TUnitItem>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TUnitItem| &row.id,
    )
    .context("Failed to parse table update for table \"unit_item\"")
}

/// Access to the `id` unique index on the table `unit_item`,
/// which allows point queries on the field of the same name
/// via the [`UnitItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.unit_item().id().find(...)`.
pub struct UnitItemIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TUnitItem, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> UnitItemTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `unit_item`.
    pub fn id(&self) -> UnitItemIdUnique<'ctx> {
        UnitItemIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> UnitItemIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TUnitItem> {
        self.imp.find(col_val)
    }
}
