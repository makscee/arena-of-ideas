// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct CraftHero {
    pub base: String,
    pub use_rainbow: u32,
}

impl __sdk::spacetime_module::InModule for CraftHero {
    type Module = super::RemoteModule;
}

pub struct CraftHeroCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `craft_hero`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait craft_hero {
    /// Request that the remote module invoke the reducer `craft_hero` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_craft_hero`] callbacks.
    fn craft_hero(&self, base: String, use_rainbow: u32) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `craft_hero`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`CraftHeroCallbackId`] can be passed to [`Self::remove_on_craft_hero`]
    /// to cancel the callback.
    fn on_craft_hero(
        &self,
        callback: impl FnMut(&super::EventContext, &String, &u32) + Send + 'static,
    ) -> CraftHeroCallbackId;
    /// Cancel a callback previously registered by [`Self::on_craft_hero`],
    /// causing it not to run in the future.
    fn remove_on_craft_hero(&self, callback: CraftHeroCallbackId);
}

impl craft_hero for super::RemoteReducers {
    fn craft_hero(&self, base: String, use_rainbow: u32) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("craft_hero", CraftHero { base, use_rainbow })
    }
    fn on_craft_hero(
        &self,
        mut callback: impl FnMut(&super::EventContext, &String, &u32) + Send + 'static,
    ) -> CraftHeroCallbackId {
        CraftHeroCallbackId(self.imp.on_reducer::<CraftHero>(
            "craft_hero",
            Box::new(move |ctx: &super::EventContext, args: &CraftHero| {
                callback(ctx, &args.base, &args.use_rainbow)
            }),
        ))
    }
    fn remove_on_craft_hero(&self, callback: CraftHeroCallbackId) {
        self.imp
            .remove_on_reducer::<CraftHero>("craft_hero", callback.0)
    }
}
