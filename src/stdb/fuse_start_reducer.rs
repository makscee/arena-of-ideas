// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct FuseStart {
    pub a: u8,
    pub b: u8,
}

impl __sdk::spacetime_module::InModule for FuseStart {
    type Module = super::RemoteModule;
}

pub struct FuseStartCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fuse_start`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fuse_start {
    /// Request that the remote module invoke the reducer `fuse_start` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_fuse_start`] callbacks.
    fn fuse_start(&self, a: u8, b: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `fuse_start`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`FuseStartCallbackId`] can be passed to [`Self::remove_on_fuse_start`]
    /// to cancel the callback.
    fn on_fuse_start(
        &self,
        callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> FuseStartCallbackId;
    /// Cancel a callback previously registered by [`Self::on_fuse_start`],
    /// causing it not to run in the future.
    fn remove_on_fuse_start(&self, callback: FuseStartCallbackId);
}

impl fuse_start for super::RemoteReducers {
    fn fuse_start(&self, a: u8, b: u8) -> __anyhow::Result<()> {
        self.imp.call_reducer("fuse_start", FuseStart { a, b })
    }
    fn on_fuse_start(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> FuseStartCallbackId {
        FuseStartCallbackId(self.imp.on_reducer::<FuseStart>(
            "fuse_start",
            Box::new(move |ctx: &super::EventContext, args: &FuseStart| {
                callback(ctx, &args.a, &args.b)
            }),
        ))
    }
    fn remove_on_fuse_start(&self, callback: FuseStartCallbackId) {
        self.imp
            .remove_on_reducer::<FuseStart>("fuse_start", callback.0)
    }
}
