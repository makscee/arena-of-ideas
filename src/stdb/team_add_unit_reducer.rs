// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct TeamAddUnit {
    pub team: u64,
    pub unit: u64,
}

impl __sdk::spacetime_module::InModule for TeamAddUnit {
    type Module = super::RemoteModule;
}

pub struct TeamAddUnitCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `team_add_unit`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait team_add_unit {
    /// Request that the remote module invoke the reducer `team_add_unit` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_team_add_unit`] callbacks.
    fn team_add_unit(&self, team: u64, unit: u64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `team_add_unit`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`TeamAddUnitCallbackId`] can be passed to [`Self::remove_on_team_add_unit`]
    /// to cancel the callback.
    fn on_team_add_unit(
        &self,
        callback: impl FnMut(&super::EventContext, &u64, &u64) + Send + 'static,
    ) -> TeamAddUnitCallbackId;
    /// Cancel a callback previously registered by [`Self::on_team_add_unit`],
    /// causing it not to run in the future.
    fn remove_on_team_add_unit(&self, callback: TeamAddUnitCallbackId);
}

impl team_add_unit for super::RemoteReducers {
    fn team_add_unit(&self, team: u64, unit: u64) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("team_add_unit", TeamAddUnit { team, unit })
    }
    fn on_team_add_unit(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64, &u64) + Send + 'static,
    ) -> TeamAddUnitCallbackId {
        TeamAddUnitCallbackId(self.imp.on_reducer::<TeamAddUnit>(
            "team_add_unit",
            Box::new(move |ctx: &super::EventContext, args: &TeamAddUnit| {
                callback(ctx, &args.team, &args.unit)
            }),
        ))
    }
    fn remove_on_team_add_unit(&self, callback: TeamAddUnitCallbackId) {
        self.imp
            .remove_on_reducer::<TeamAddUnit>("team_add_unit", callback.0)
    }
}
