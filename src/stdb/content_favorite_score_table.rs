// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_content_favorite_score_type::TContentFavoriteScore;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `content_favorite_score`.
///
/// Obtain a handle from the [`ContentFavoriteScoreTableAccess::content_favorite_score`] method on [`super::RemoteTables`],
/// like `ctx.db.content_favorite_score()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_favorite_score().on_insert(...)`.
pub struct ContentFavoriteScoreTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TContentFavoriteScore>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `content_favorite_score`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ContentFavoriteScoreTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ContentFavoriteScoreTableHandle`], which mediates access to the table `content_favorite_score`.
    fn content_favorite_score(&self) -> ContentFavoriteScoreTableHandle<'_>;
}

impl ContentFavoriteScoreTableAccess for super::RemoteTables {
    fn content_favorite_score(&self) -> ContentFavoriteScoreTableHandle<'_> {
        ContentFavoriteScoreTableHandle {
            imp: self
                .imp
                .get_table::<TContentFavoriteScore>("content_favorite_score"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ContentFavoriteScoreInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct ContentFavoriteScoreDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for ContentFavoriteScoreTableHandle<'ctx> {
    type Row = TContentFavoriteScore;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TContentFavoriteScore> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ContentFavoriteScoreInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentFavoriteScoreInsertCallbackId {
        ContentFavoriteScoreInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ContentFavoriteScoreInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ContentFavoriteScoreDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentFavoriteScoreDeleteCallbackId {
        ContentFavoriteScoreDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ContentFavoriteScoreDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ContentFavoriteScoreUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for ContentFavoriteScoreTableHandle<'ctx> {
    type UpdateCallbackId = ContentFavoriteScoreUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ContentFavoriteScoreUpdateCallbackId {
        ContentFavoriteScoreUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ContentFavoriteScoreUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TContentFavoriteScore>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<String>(
        raw_updates,
        |row: &TContentFavoriteScore| &row.type_target,
    )
    .context("Failed to parse table update for table \"content_favorite_score\"")
}

/// Access to the `type_target` unique index on the table `content_favorite_score`,
/// which allows point queries on the field of the same name
/// via the [`ContentFavoriteScoreTypeTargetUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_favorite_score().type_target().find(...)`.
pub struct ContentFavoriteScoreTypeTargetUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TContentFavoriteScore, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ContentFavoriteScoreTableHandle<'ctx> {
    /// Get a handle on the `type_target` unique index on the table `content_favorite_score`.
    pub fn type_target(&self) -> ContentFavoriteScoreTypeTargetUnique<'ctx> {
        ContentFavoriteScoreTypeTargetUnique {
            imp: self
                .imp
                .get_unique_constraint::<String>("type_target", |row| &row.type_target),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ContentFavoriteScoreTypeTargetUnique<'ctx> {
    /// Find the subscribed row whose `type_target` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TContentFavoriteScore> {
        self.imp.find(col_val)
    }
}
