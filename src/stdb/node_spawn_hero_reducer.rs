// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::__codegen::{
    self as __sdk, __lib, __sats, __ws,
    anyhow::{self as __anyhow, Context as _},
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct NodeSpawnHeroArgs {
    pub name: String,
}

impl From<NodeSpawnHeroArgs> for super::Reducer {
    fn from(args: NodeSpawnHeroArgs) -> Self {
        Self::NodeSpawnHero { name: args.name }
    }
}

impl __sdk::InModule for NodeSpawnHeroArgs {
    type Module = super::RemoteModule;
}

pub struct NodeSpawnHeroCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `node_spawn_hero`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait node_spawn_hero {
    /// Request that the remote module invoke the reducer `node_spawn_hero` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_node_spawn_hero`] callbacks.
    fn node_spawn_hero(&self, name: String) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `node_spawn_hero`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`NodeSpawnHeroCallbackId`] can be passed to [`Self::remove_on_node_spawn_hero`]
    /// to cancel the callback.
    fn on_node_spawn_hero(
        &self,
        callback: impl FnMut(&super::EventContext, &String) + Send + 'static,
    ) -> NodeSpawnHeroCallbackId;
    /// Cancel a callback previously registered by [`Self::on_node_spawn_hero`],
    /// causing it not to run in the future.
    fn remove_on_node_spawn_hero(&self, callback: NodeSpawnHeroCallbackId);
}

impl node_spawn_hero for super::RemoteReducers {
    fn node_spawn_hero(&self, name: String) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("node_spawn_hero", NodeSpawnHeroArgs { name })
    }
    fn on_node_spawn_hero(
        &self,
        mut callback: impl FnMut(&super::EventContext, &String) + Send + 'static,
    ) -> NodeSpawnHeroCallbackId {
        NodeSpawnHeroCallbackId(self.imp.on_reducer(
            "node_spawn_hero",
            Box::new(move |ctx: &super::EventContext| {
                let super::EventContext {
                    event:
                        __sdk::Event::Reducer(__sdk::ReducerEvent {
                            reducer: super::Reducer::NodeSpawnHero { name },
                            ..
                        }),
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, name)
            }),
        ))
    }
    fn remove_on_node_spawn_hero(&self, callback: NodeSpawnHeroCallbackId) {
        self.imp.remove_on_reducer("node_spawn_hero", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `node_spawn_hero`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_node_spawn_hero {
    /// Set the call-reducer flags for the reducer `node_spawn_hero` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn node_spawn_hero(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_node_spawn_hero for super::SetReducerFlags {
    fn node_spawn_hero(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("node_spawn_hero", flags);
    }
}
