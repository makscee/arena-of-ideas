// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::t_incubator_source_type::TIncubatorSource;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `incubator_source`.
///
/// Obtain a handle from the [`IncubatorSourceTableAccess::incubator_source`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_source()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_source().on_insert(...)`.
pub struct IncubatorSourceTableHandle<'ctx> {
    imp: __sdk::TableHandle<TIncubatorSource>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_source`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorSourceTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorSourceTableHandle`], which mediates access to the table `incubator_source`.
    fn incubator_source(&self) -> IncubatorSourceTableHandle<'_>;
}

impl IncubatorSourceTableAccess for super::RemoteTables {
    fn incubator_source(&self) -> IncubatorSourceTableHandle<'_> {
        IncubatorSourceTableHandle {
            imp: self.imp.get_table::<TIncubatorSource>("incubator_source"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorSourceInsertCallbackId(__sdk::CallbackId);
pub struct IncubatorSourceDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for IncubatorSourceTableHandle<'ctx> {
    type Row = TIncubatorSource;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorSource> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorSourceInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorSourceInsertCallbackId {
        IncubatorSourceInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorSourceInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorSourceDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorSourceDeleteCallbackId {
        IncubatorSourceDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorSourceDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TIncubatorSource>("incubator_source");
    _table.add_unique_constraint::<u64>("node_id", |row| &row.node_id);
}
pub struct IncubatorSourceUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for IncubatorSourceTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorSourceUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorSourceUpdateCallbackId {
        IncubatorSourceUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorSourceUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<TIncubatorSource>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TIncubatorSource>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `node_id` unique index on the table `incubator_source`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorSourceNodeIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_source().node_id().find(...)`.
pub struct IncubatorSourceNodeIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TIncubatorSource, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorSourceTableHandle<'ctx> {
    /// Get a handle on the `node_id` unique index on the table `incubator_source`.
    pub fn node_id(&self) -> IncubatorSourceNodeIdUnique<'ctx> {
        IncubatorSourceNodeIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("node_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorSourceNodeIdUnique<'ctx> {
    /// Find the subscribed row whose `node_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorSource> {
        self.imp.find(col_val)
    }
}
