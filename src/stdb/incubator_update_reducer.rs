// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

use super::t_base_unit_type::TBaseUnit;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct IncubatorUpdate {
    pub id: u64,
    pub unit: TBaseUnit,
}

impl __sdk::spacetime_module::InModule for IncubatorUpdate {
    type Module = super::RemoteModule;
}

pub struct IncubatorUpdateCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `incubator_update`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait incubator_update {
    /// Request that the remote module invoke the reducer `incubator_update` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_incubator_update`] callbacks.
    fn incubator_update(&self, id: u64, unit: TBaseUnit) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `incubator_update`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`IncubatorUpdateCallbackId`] can be passed to [`Self::remove_on_incubator_update`]
    /// to cancel the callback.
    fn on_incubator_update(
        &self,
        callback: impl FnMut(&super::EventContext, &u64, &TBaseUnit) + Send + 'static,
    ) -> IncubatorUpdateCallbackId;
    /// Cancel a callback previously registered by [`Self::on_incubator_update`],
    /// causing it not to run in the future.
    fn remove_on_incubator_update(&self, callback: IncubatorUpdateCallbackId);
}

impl incubator_update for super::RemoteReducers {
    fn incubator_update(&self, id: u64, unit: TBaseUnit) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("incubator_update", IncubatorUpdate { id, unit })
    }
    fn on_incubator_update(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64, &TBaseUnit) + Send + 'static,
    ) -> IncubatorUpdateCallbackId {
        IncubatorUpdateCallbackId(self.imp.on_reducer::<IncubatorUpdate>(
            "incubator_update",
            Box::new(move |ctx: &super::EventContext, args: &IncubatorUpdate| {
                callback(ctx, &args.id, &args.unit)
            }),
        ))
    }
    fn remove_on_incubator_update(&self, callback: IncubatorUpdateCallbackId) {
        self.imp
            .remove_on_reducer::<IncubatorUpdate>("incubator_update", callback.0)
    }
}
