// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

use super::fusion_type::Fusion;
use super::game_mode_type::GameMode;
use super::reward_type::Reward;
use super::shop_slot_type::ShopSlot;
use super::team_slot_type::TeamSlot;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct TArenaRun {
    pub mode: GameMode,
    pub id: u64,
    pub owner: u64,
    pub team: u64,
    pub battles: Vec<u64>,
    pub shop_slots: Vec<ShopSlot>,
    pub team_slots: Vec<TeamSlot>,
    pub fusion: Option<Fusion>,
    pub g: i32,
    pub price_reroll: i32,
    pub free_rerolls: u32,
    pub lives: u32,
    pub max_lives: u32,
    pub replenish_lives: u32,
    pub active: bool,
    pub boss_team: u64,
    pub boss_floor: u32,
    pub current_floor_boss: Option<u64>,
    pub floor: u32,
    pub rerolls: u32,
    pub rewards: Vec<Reward>,
    pub streak: u32,
    pub weights: Vec<i32>,
    pub last_updated: u64,
}

impl __sdk::spacetime_module::InModule for TArenaRun {
    type Module = super::RemoteModule;
}
