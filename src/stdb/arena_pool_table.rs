// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::game_mode_type::GameMode;
use super::t_arena_pool_type::TArenaPool;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `arena_pool`.
///
/// Obtain a handle from the [`ArenaPoolTableAccess::arena_pool`] method on [`super::RemoteTables`],
/// like `ctx.db.arena_pool()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.arena_pool().on_insert(...)`.
pub struct ArenaPoolTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TArenaPool>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `arena_pool`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ArenaPoolTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ArenaPoolTableHandle`], which mediates access to the table `arena_pool`.
    fn arena_pool(&self) -> ArenaPoolTableHandle<'_>;
}

impl ArenaPoolTableAccess for super::RemoteTables {
    fn arena_pool(&self) -> ArenaPoolTableHandle<'_> {
        ArenaPoolTableHandle {
            imp: self.imp.get_table::<TArenaPool>("arena_pool"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ArenaPoolInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct ArenaPoolDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for ArenaPoolTableHandle<'ctx> {
    type Row = TArenaPool;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TArenaPool> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ArenaPoolInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ArenaPoolInsertCallbackId {
        ArenaPoolInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ArenaPoolInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ArenaPoolDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ArenaPoolDeleteCallbackId {
        ArenaPoolDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ArenaPoolDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ArenaPoolUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for ArenaPoolTableHandle<'ctx> {
    type UpdateCallbackId = ArenaPoolUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ArenaPoolUpdateCallbackId {
        ArenaPoolUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ArenaPoolUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TArenaPool>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TArenaPool| &row.team,
    )
    .context("Failed to parse table update for table \"arena_pool\"")
}

/// Access to the `team` unique index on the table `arena_pool`,
/// which allows point queries on the field of the same name
/// via the [`ArenaPoolTeamUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.arena_pool().team().find(...)`.
pub struct ArenaPoolTeamUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TArenaPool, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ArenaPoolTableHandle<'ctx> {
    /// Get a handle on the `team` unique index on the table `arena_pool`.
    pub fn team(&self) -> ArenaPoolTeamUnique<'ctx> {
        ArenaPoolTeamUnique {
            imp: self
                .imp
                .get_unique_constraint::<u64>("team", |row| &row.team),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ArenaPoolTeamUnique<'ctx> {
    /// Find the subscribed row whose `team` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TArenaPool> {
        self.imp.find(col_val)
    }
}
