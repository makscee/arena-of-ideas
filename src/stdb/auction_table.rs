// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::item_kind_type::ItemKind;
use super::t_auction_type::TAuction;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `auction`.
///
/// Obtain a handle from the [`AuctionTableAccess::auction`] method on [`super::RemoteTables`],
/// like `ctx.db.auction()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.auction().on_insert(...)`.
pub struct AuctionTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TAuction>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `auction`.
///
/// Implemented for [`super::RemoteTables`].
pub trait AuctionTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`AuctionTableHandle`], which mediates access to the table `auction`.
    fn auction(&self) -> AuctionTableHandle<'_>;
}

impl AuctionTableAccess for super::RemoteTables {
    fn auction(&self) -> AuctionTableHandle<'_> {
        AuctionTableHandle {
            imp: self.imp.get_table::<TAuction>("auction"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct AuctionInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct AuctionDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for AuctionTableHandle<'ctx> {
    type Row = TAuction;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TAuction> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = AuctionInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> AuctionInsertCallbackId {
        AuctionInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: AuctionInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = AuctionDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> AuctionDeleteCallbackId {
        AuctionDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: AuctionDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct AuctionUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for AuctionTableHandle<'ctx> {
    type UpdateCallbackId = AuctionUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> AuctionUpdateCallbackId {
        AuctionUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: AuctionUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TAuction>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TAuction| &row.item_id,
    )
    .context("Failed to parse table update for table \"auction\"")
}

/// Access to the `item_id` unique index on the table `auction`,
/// which allows point queries on the field of the same name
/// via the [`AuctionItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.auction().item_id().find(...)`.
pub struct AuctionItemIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TAuction, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> AuctionTableHandle<'ctx> {
    /// Get a handle on the `item_id` unique index on the table `auction`.
    pub fn item_id(&self) -> AuctionItemIdUnique<'ctx> {
        AuctionItemIdUnique {
            imp: self
                .imp
                .get_unique_constraint::<u64>("item_id", |row| &row.item_id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> AuctionItemIdUnique<'ctx> {
    /// Find the subscribed row whose `item_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TAuction> {
        self.imp.find(col_val)
    }
}
