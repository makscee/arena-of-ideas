// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

use super::s_incubator_type_type::SIncubatorType;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct IncubatorLinkAdd {
    pub from: String,
    pub from_type: SIncubatorType,
    pub to: String,
    pub to_type: SIncubatorType,
}

impl __sdk::spacetime_module::InModule for IncubatorLinkAdd {
    type Module = super::RemoteModule;
}

pub struct IncubatorLinkAddCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `incubator_link_add`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait incubator_link_add {
    /// Request that the remote module invoke the reducer `incubator_link_add` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_incubator_link_add`] callbacks.
    fn incubator_link_add(
        &self,
        from: String,
        from_type: SIncubatorType,
        to: String,
        to_type: SIncubatorType,
    ) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `incubator_link_add`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`IncubatorLinkAddCallbackId`] can be passed to [`Self::remove_on_incubator_link_add`]
    /// to cancel the callback.
    fn on_incubator_link_add(
        &self,
        callback: impl FnMut(&super::EventContext, &String, &SIncubatorType, &String, &SIncubatorType)
            + Send
            + 'static,
    ) -> IncubatorLinkAddCallbackId;
    /// Cancel a callback previously registered by [`Self::on_incubator_link_add`],
    /// causing it not to run in the future.
    fn remove_on_incubator_link_add(&self, callback: IncubatorLinkAddCallbackId);
}

impl incubator_link_add for super::RemoteReducers {
    fn incubator_link_add(
        &self,
        from: String,
        from_type: SIncubatorType,
        to: String,
        to_type: SIncubatorType,
    ) -> __anyhow::Result<()> {
        self.imp.call_reducer(
            "incubator_link_add",
            IncubatorLinkAdd {
                from,
                from_type,
                to,
                to_type,
            },
        )
    }
    fn on_incubator_link_add(
        &self,
        mut callback: impl FnMut(&super::EventContext, &String, &SIncubatorType, &String, &SIncubatorType)
            + Send
            + 'static,
    ) -> IncubatorLinkAddCallbackId {
        IncubatorLinkAddCallbackId(self.imp.on_reducer::<IncubatorLinkAdd>(
            "incubator_link_add",
            Box::new(move |ctx: &super::EventContext, args: &IncubatorLinkAdd| {
                callback(ctx, &args.from, &args.from_type, &args.to, &args.to_type)
            }),
        ))
    }
    fn remove_on_incubator_link_add(&self, callback: IncubatorLinkAddCallbackId) {
        self.imp
            .remove_on_reducer::<IncubatorLinkAdd>("incubator_link_add", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `incubator_link_add`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_incubator_link_add {
    /// Set the call-reducer flags for the reducer `incubator_link_add` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn incubator_link_add(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_incubator_link_add for super::SetReducerFlags {
    fn incubator_link_add(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("incubator_link_add", flags);
    }
}
