// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_status_type::TIncubatorStatus;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_status`.
///
/// Obtain a handle from the [`IncubatorStatusTableAccess::incubator_status`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_status()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_status().on_insert(...)`.
pub struct IncubatorStatusTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorStatus>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_status`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorStatusTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorStatusTableHandle`], which mediates access to the table `incubator_status`.
    fn incubator_status(&self) -> IncubatorStatusTableHandle<'_>;
}

impl IncubatorStatusTableAccess for super::RemoteTables {
    fn incubator_status(&self) -> IncubatorStatusTableHandle<'_> {
        IncubatorStatusTableHandle {
            imp: self.imp.get_table::<TIncubatorStatus>("incubator_status"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorStatusInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorStatusDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorStatusTableHandle<'ctx> {
    type Row = TIncubatorStatus;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorStatus> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorStatusInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusInsertCallbackId {
        IncubatorStatusInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorStatusInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorStatusDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusDeleteCallbackId {
        IncubatorStatusDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorStatusDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorStatusUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorStatusTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorStatusUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorStatusUpdateCallbackId {
        IncubatorStatusUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorStatusUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorStatus>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorStatus| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_status\"")
}

/// Access to the `id` unique index on the table `incubator_status`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorStatusIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_status().id().find(...)`.
pub struct IncubatorStatusIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorStatus, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorStatusTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_status`.
    pub fn id(&self) -> IncubatorStatusIdUnique<'ctx> {
        IncubatorStatusIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorStatusIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorStatus> {
        self.imp.find(col_val)
    }
}
