// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

use super::global_data_type::GlobalData;
use super::global_settings_type::GlobalSettings;
use super::t_ability_type::TAbility;
use super::t_arena_leaderboard_type::TArenaLeaderboard;
use super::t_arena_run_archive_type::TArenaRunArchive;
use super::t_arena_run_type::TArenaRun;
use super::t_auction_type::TAuction;
use super::t_base_unit_type::TBaseUnit;
use super::t_battle_type::TBattle;
use super::t_daily_state_type::TDailyState;
use super::t_global_event_type::TGlobalEvent;
use super::t_house_type::THouse;
use super::t_incubator_favorite_type::TIncubatorFavorite;
use super::t_incubator_type::TIncubator;
use super::t_incubator_vote_type::TIncubatorVote;
use super::t_lootbox_item_type::TLootboxItem;
use super::t_meta_shop_type::TMetaShop;
use super::t_player_game_stats_type::TPlayerGameStats;
use super::t_player_stats_type::TPlayerStats;
use super::t_player_type::TPlayer;
use super::t_quest_type::TQuest;
use super::t_rainbow_shard_item_type::TRainbowShardItem;
use super::t_status_type::TStatus;
use super::t_team_type::TTeam;
use super::t_trade_type::TTrade;
use super::t_unit_balance_type::TUnitBalance;
use super::t_unit_item_type::TUnitItem;
use super::t_unit_shard_item_type::TUnitShardItem;
use super::t_wallet_type::TWallet;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct GameData {
    pub global_data: Vec<GlobalData>,
    pub global_settings: Vec<GlobalSettings>,
    pub ability: Vec<TAbility>,
    pub arena_leaderboard: Vec<TArenaLeaderboard>,
    pub arena_run: Vec<TArenaRun>,
    pub arena_run_archive: Vec<TArenaRunArchive>,
    pub auction: Vec<TAuction>,
    pub base_unit: Vec<TBaseUnit>,
    pub battle: Vec<TBattle>,
    pub daily_state: Vec<TDailyState>,
    pub house: Vec<THouse>,
    pub lootbox_item: Vec<TLootboxItem>,
    pub meta_shop: Vec<TMetaShop>,
    pub quest: Vec<TQuest>,
    pub rainbow_shard_item: Vec<TRainbowShardItem>,
    pub status: Vec<TStatus>,
    pub team: Vec<TTeam>,
    pub trade: Vec<TTrade>,
    pub unit_balance: Vec<TUnitBalance>,
    pub unit_item: Vec<TUnitItem>,
    pub unit_shard_item: Vec<TUnitShardItem>,
    pub player: Vec<TPlayer>,
    pub player_stats: Vec<TPlayerStats>,
    pub player_game_stats: Vec<TPlayerGameStats>,
    pub wallet: Vec<TWallet>,
    pub incubator: Vec<TIncubator>,
    pub incubator_vote: Vec<TIncubatorVote>,
    pub incubator_favorite: Vec<TIncubatorFavorite>,
    pub global_event: Vec<TGlobalEvent>,
}

impl __sdk::spacetime_module::InModule for GameData {
    type Module = super::RemoteModule;
}
