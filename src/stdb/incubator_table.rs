// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_base_unit_type::TBaseUnit;
use super::t_incubator_type::TIncubator;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator`.
///
/// Obtain a handle from the [`IncubatorTableAccess::incubator`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator().on_insert(...)`.
pub struct IncubatorTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubator>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorTableHandle`], which mediates access to the table `incubator`.
    fn incubator(&self) -> IncubatorTableHandle<'_>;
}

impl IncubatorTableAccess for super::RemoteTables {
    fn incubator(&self) -> IncubatorTableHandle<'_> {
        IncubatorTableHandle {
            imp: self.imp.get_table::<TIncubator>("incubator"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorTableHandle<'ctx> {
    type Row = TIncubator;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubator> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorInsertCallbackId {
        IncubatorInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorDeleteCallbackId {
        IncubatorDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorUpdateCallbackId {
        IncubatorUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubator>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubator| &row.id,
    )
    .context("Failed to parse table update for table \"incubator\"")
}

/// Access to the `id` unique index on the table `incubator`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator().id().find(...)`.
pub struct IncubatorIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubator, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator`.
    pub fn id(&self) -> IncubatorIdUnique<'ctx> {
        IncubatorIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubator> {
        self.imp.find(col_val)
    }
}
