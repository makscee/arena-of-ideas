// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct TeamDisband {
    pub team: u64,
}

impl __sdk::spacetime_module::InModule for TeamDisband {
    type Module = super::RemoteModule;
}

pub struct TeamDisbandCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `team_disband`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait team_disband {
    /// Request that the remote module invoke the reducer `team_disband` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_team_disband`] callbacks.
    fn team_disband(&self, team: u64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `team_disband`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`TeamDisbandCallbackId`] can be passed to [`Self::remove_on_team_disband`]
    /// to cancel the callback.
    fn on_team_disband(
        &self,
        callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> TeamDisbandCallbackId;
    /// Cancel a callback previously registered by [`Self::on_team_disband`],
    /// causing it not to run in the future.
    fn remove_on_team_disband(&self, callback: TeamDisbandCallbackId);
}

impl team_disband for super::RemoteReducers {
    fn team_disband(&self, team: u64) -> __anyhow::Result<()> {
        self.imp.call_reducer("team_disband", TeamDisband { team })
    }
    fn on_team_disband(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> TeamDisbandCallbackId {
        TeamDisbandCallbackId(self.imp.on_reducer::<TeamDisband>(
            "team_disband",
            Box::new(move |ctx: &super::EventContext, args: &TeamDisband| {
                callback(ctx, &args.team)
            }),
        ))
    }
    fn remove_on_team_disband(&self, callback: TeamDisbandCallbackId) {
        self.imp
            .remove_on_reducer::<TeamDisband>("team_disband", callback.0)
    }
}
