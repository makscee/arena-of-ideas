// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct StackTeam {
    pub source: u8,
    pub target: u8,
}

impl __sdk::spacetime_module::InModule for StackTeam {
    type Module = super::RemoteModule;
}

pub struct StackTeamCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stack_team`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stack_team {
    /// Request that the remote module invoke the reducer `stack_team` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_stack_team`] callbacks.
    fn stack_team(&self, source: u8, target: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `stack_team`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`StackTeamCallbackId`] can be passed to [`Self::remove_on_stack_team`]
    /// to cancel the callback.
    fn on_stack_team(
        &self,
        callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> StackTeamCallbackId;
    /// Cancel a callback previously registered by [`Self::on_stack_team`],
    /// causing it not to run in the future.
    fn remove_on_stack_team(&self, callback: StackTeamCallbackId);
}

impl stack_team for super::RemoteReducers {
    fn stack_team(&self, source: u8, target: u8) -> __anyhow::Result<()> {
        self.imp
            .call_reducer("stack_team", StackTeam { source, target })
    }
    fn on_stack_team(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8, &u8) + Send + 'static,
    ) -> StackTeamCallbackId {
        StackTeamCallbackId(self.imp.on_reducer::<StackTeam>(
            "stack_team",
            Box::new(move |ctx: &super::EventContext, args: &StackTeam| {
                callback(ctx, &args.source, &args.target)
            }),
        ))
    }
    fn remove_on_stack_team(&self, callback: StackTeamCallbackId) {
        self.imp
            .remove_on_reducer::<StackTeam>("stack_team", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `stack_team`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_stack_team {
    /// Set the call-reducer flags for the reducer `stack_team` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn stack_team(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_stack_team for super::SetReducerFlags {
    fn stack_team(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("stack_team", flags);
    }
}
