// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_node_type::TNode;
use spacetimedb_sdk::__codegen::{
    self as __sdk, __lib, __sats, __ws,
    anyhow::{self as __anyhow, Context as _},
};

/// Table handle for the table `tnodes`.
///
/// Obtain a handle from the [`TnodesTableAccess::tnodes`] method on [`super::RemoteTables`],
/// like `ctx.db.tnodes()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tnodes().on_insert(...)`.
pub struct TnodesTableHandle<'ctx> {
    imp: __sdk::TableHandle<TNode>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `tnodes`.
///
/// Implemented for [`super::RemoteTables`].
pub trait TnodesTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`TnodesTableHandle`], which mediates access to the table `tnodes`.
    fn tnodes(&self) -> TnodesTableHandle<'_>;
}

impl TnodesTableAccess for super::RemoteTables {
    fn tnodes(&self) -> TnodesTableHandle<'_> {
        TnodesTableHandle {
            imp: self.imp.get_table::<TNode>("tnodes"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct TnodesInsertCallbackId(__sdk::CallbackId);
pub struct TnodesDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for TnodesTableHandle<'ctx> {
    type Row = TNode;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TNode> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = TnodesInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TnodesInsertCallbackId {
        TnodesInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: TnodesInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = TnodesDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TnodesDeleteCallbackId {
        TnodesDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: TnodesDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TNode>("tnodes");
    _table.add_unique_constraint::<String>("key", |row| &row.key);
    _table.add_unique_constraint::<String>("data", |row| &row.data);
}
pub struct TnodesUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for TnodesTableHandle<'ctx> {
    type UpdateCallbackId = TnodesUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> TnodesUpdateCallbackId {
        TnodesUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: TnodesUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::TableUpdate<TNode>> {
    __sdk::TableUpdate::parse_table_update_with_primary_key::<String>(raw_updates, |row: &TNode| {
        &row.key
    })
    .context("Failed to parse table update for table \"tnodes\"")
}

/// Access to the `key` unique index on the table `tnodes`,
/// which allows point queries on the field of the same name
/// via the [`TnodesKeyUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tnodes().key().find(...)`.
pub struct TnodesKeyUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TNode, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TnodesTableHandle<'ctx> {
    /// Get a handle on the `key` unique index on the table `tnodes`.
    pub fn key(&self) -> TnodesKeyUnique<'ctx> {
        TnodesKeyUnique {
            imp: self.imp.get_unique_constraint::<String>("key"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TnodesKeyUnique<'ctx> {
    /// Find the subscribed row whose `key` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TNode> {
        self.imp.find(col_val)
    }
}

/// Access to the `data` unique index on the table `tnodes`,
/// which allows point queries on the field of the same name
/// via the [`TnodesDataUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tnodes().data().find(...)`.
pub struct TnodesDataUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TNode, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TnodesTableHandle<'ctx> {
    /// Get a handle on the `data` unique index on the table `tnodes`.
    pub fn data(&self) -> TnodesDataUnique<'ctx> {
        TnodesDataUnique {
            imp: self.imp.get_unique_constraint::<String>("data"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TnodesDataUnique<'ctx> {
    /// Find the subscribed row whose `data` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TNode> {
        self.imp.find(col_val)
    }
}
