// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::lootbox_kind_type::LootboxKind;
use super::t_lootbox_item_type::TLootboxItem;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `lootbox_item`.
///
/// Obtain a handle from the [`LootboxItemTableAccess::lootbox_item`] method on [`super::RemoteTables`],
/// like `ctx.db.lootbox_item()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.lootbox_item().on_insert(...)`.
pub struct LootboxItemTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TLootboxItem>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `lootbox_item`.
///
/// Implemented for [`super::RemoteTables`].
pub trait LootboxItemTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`LootboxItemTableHandle`], which mediates access to the table `lootbox_item`.
    fn lootbox_item(&self) -> LootboxItemTableHandle<'_>;
}

impl LootboxItemTableAccess for super::RemoteTables {
    fn lootbox_item(&self) -> LootboxItemTableHandle<'_> {
        LootboxItemTableHandle {
            imp: self.imp.get_table::<TLootboxItem>("lootbox_item"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct LootboxItemInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct LootboxItemDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for LootboxItemTableHandle<'ctx> {
    type Row = TLootboxItem;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TLootboxItem> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = LootboxItemInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootboxItemInsertCallbackId {
        LootboxItemInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: LootboxItemInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = LootboxItemDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootboxItemDeleteCallbackId {
        LootboxItemDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: LootboxItemDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct LootboxItemUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for LootboxItemTableHandle<'ctx> {
    type UpdateCallbackId = LootboxItemUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> LootboxItemUpdateCallbackId {
        LootboxItemUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: LootboxItemUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TLootboxItem>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TLootboxItem| &row.id,
    )
    .context("Failed to parse table update for table \"lootbox_item\"")
}

/// Access to the `id` unique index on the table `lootbox_item`,
/// which allows point queries on the field of the same name
/// via the [`LootboxItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.lootbox_item().id().find(...)`.
pub struct LootboxItemIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TLootboxItem, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> LootboxItemTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `lootbox_item`.
    pub fn id(&self) -> LootboxItemIdUnique<'ctx> {
        LootboxItemIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> LootboxItemIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TLootboxItem> {
        self.imp.find(col_val)
    }
}
