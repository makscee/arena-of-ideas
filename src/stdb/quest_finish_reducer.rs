// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct QuestFinish {
    pub id: u64,
}

impl __sdk::spacetime_module::InModule for QuestFinish {
    type Module = super::RemoteModule;
}

pub struct QuestFinishCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `quest_finish`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait quest_finish {
    /// Request that the remote module invoke the reducer `quest_finish` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_quest_finish`] callbacks.
    fn quest_finish(&self, id: u64) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `quest_finish`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`QuestFinishCallbackId`] can be passed to [`Self::remove_on_quest_finish`]
    /// to cancel the callback.
    fn on_quest_finish(
        &self,
        callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> QuestFinishCallbackId;
    /// Cancel a callback previously registered by [`Self::on_quest_finish`],
    /// causing it not to run in the future.
    fn remove_on_quest_finish(&self, callback: QuestFinishCallbackId);
}

impl quest_finish for super::RemoteReducers {
    fn quest_finish(&self, id: u64) -> __anyhow::Result<()> {
        self.imp.call_reducer("quest_finish", QuestFinish { id })
    }
    fn on_quest_finish(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u64) + Send + 'static,
    ) -> QuestFinishCallbackId {
        QuestFinishCallbackId(self.imp.on_reducer::<QuestFinish>(
            "quest_finish",
            Box::new(move |ctx: &super::EventContext, args: &QuestFinish| callback(ctx, &args.id)),
        ))
    }
    fn remove_on_quest_finish(&self, callback: QuestFinishCallbackId) {
        self.imp
            .remove_on_reducer::<QuestFinish>("quest_finish", callback.0)
    }
}
