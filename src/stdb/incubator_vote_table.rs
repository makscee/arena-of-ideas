// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_vote_type::TIncubatorVote;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_vote`.
///
/// Obtain a handle from the [`IncubatorVoteTableAccess::incubator_vote`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_vote()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_vote().on_insert(...)`.
pub struct IncubatorVoteTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorVote>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_vote`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorVoteTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorVoteTableHandle`], which mediates access to the table `incubator_vote`.
    fn incubator_vote(&self) -> IncubatorVoteTableHandle<'_>;
}

impl IncubatorVoteTableAccess for super::RemoteTables {
    fn incubator_vote(&self) -> IncubatorVoteTableHandle<'_> {
        IncubatorVoteTableHandle {
            imp: self.imp.get_table::<TIncubatorVote>("incubator_vote"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorVoteInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorVoteDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorVoteTableHandle<'ctx> {
    type Row = TIncubatorVote;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorVote> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorVoteInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorVoteInsertCallbackId {
        IncubatorVoteInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorVoteInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorVoteDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorVoteDeleteCallbackId {
        IncubatorVoteDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorVoteDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorVoteUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorVoteTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorVoteUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorVoteUpdateCallbackId {
        IncubatorVoteUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorVoteUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorVote>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorVote| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_vote\"")
}

/// Access to the `id` unique index on the table `incubator_vote`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorVoteIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_vote().id().find(...)`.
pub struct IncubatorVoteIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorVote, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorVoteTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_vote`.
    pub fn id(&self) -> IncubatorVoteIdUnique<'ctx> {
        IncubatorVoteIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorVoteIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorVote> {
        self.imp.find(col_val)
    }
}
