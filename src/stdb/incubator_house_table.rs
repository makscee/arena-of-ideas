// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_incubator_house_type::TIncubatorHouse;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `incubator_house`.
///
/// Obtain a handle from the [`IncubatorHouseTableAccess::incubator_house`] method on [`super::RemoteTables`],
/// like `ctx.db.incubator_house()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_house().on_insert(...)`.
pub struct IncubatorHouseTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TIncubatorHouse>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `incubator_house`.
///
/// Implemented for [`super::RemoteTables`].
pub trait IncubatorHouseTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`IncubatorHouseTableHandle`], which mediates access to the table `incubator_house`.
    fn incubator_house(&self) -> IncubatorHouseTableHandle<'_>;
}

impl IncubatorHouseTableAccess for super::RemoteTables {
    fn incubator_house(&self) -> IncubatorHouseTableHandle<'_> {
        IncubatorHouseTableHandle {
            imp: self.imp.get_table::<TIncubatorHouse>("incubator_house"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct IncubatorHouseInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct IncubatorHouseDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for IncubatorHouseTableHandle<'ctx> {
    type Row = TIncubatorHouse;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TIncubatorHouse> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = IncubatorHouseInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorHouseInsertCallbackId {
        IncubatorHouseInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: IncubatorHouseInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = IncubatorHouseDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> IncubatorHouseDeleteCallbackId {
        IncubatorHouseDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: IncubatorHouseDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct IncubatorHouseUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for IncubatorHouseTableHandle<'ctx> {
    type UpdateCallbackId = IncubatorHouseUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> IncubatorHouseUpdateCallbackId {
        IncubatorHouseUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: IncubatorHouseUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TIncubatorHouse>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TIncubatorHouse| &row.id,
    )
    .context("Failed to parse table update for table \"incubator_house\"")
}

/// Access to the `id` unique index on the table `incubator_house`,
/// which allows point queries on the field of the same name
/// via the [`IncubatorHouseIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.incubator_house().id().find(...)`.
pub struct IncubatorHouseIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TIncubatorHouse, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> IncubatorHouseTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `incubator_house`.
    pub fn id(&self) -> IncubatorHouseIdUnique<'ctx> {
        IncubatorHouseIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> IncubatorHouseIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TIncubatorHouse> {
        self.imp.find(col_val)
    }
}
