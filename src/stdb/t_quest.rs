// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused_imports)]
use super::game_mode::GameMode;
use super::quest_variant::QuestVariant;
use spacetimedb_sdk::{
    anyhow::{anyhow, Result},
    identity::Identity,
    reducer::{Reducer, ReducerCallbackId, Status},
    sats::{de::Deserialize, ser::Serialize},
    spacetimedb_lib,
    table::{TableIter, TableType, TableWithPrimaryKey},
    Address, ScheduleAt,
};

#[derive(Serialize, Deserialize, Clone, PartialEq, Debug)]
pub struct TQuest {
    pub id: u64,
    pub owner: u64,
    pub mode: GameMode,
    pub variant: QuestVariant,
    pub counter: u32,
    pub goal: u32,
    pub reward: i64,
    pub complete: bool,
}

impl TableType for TQuest {
    const TABLE_NAME: &'static str = "TQuest";
    type ReducerEvent = super::ReducerEvent;
}

impl TableWithPrimaryKey for TQuest {
    type PrimaryKey = u64;
    fn primary_key(&self) -> &Self::PrimaryKey {
        &self.id
    }
}

impl TQuest {
    #[allow(unused)]
    pub fn filter_by_id(id: u64) -> TableIter<Self> {
        Self::filter(|row| row.id == id)
    }
    #[allow(unused)]
    pub fn find_by_id(id: u64) -> Option<Self> {
        Self::find(|row| row.id == id)
    }
    #[allow(unused)]
    pub fn filter_by_owner(owner: u64) -> TableIter<Self> {
        Self::filter(|row| row.owner == owner)
    }
    #[allow(unused)]
    pub fn filter_by_counter(counter: u32) -> TableIter<Self> {
        Self::filter(|row| row.counter == counter)
    }
    #[allow(unused)]
    pub fn filter_by_goal(goal: u32) -> TableIter<Self> {
        Self::filter(|row| row.goal == goal)
    }
    #[allow(unused)]
    pub fn filter_by_reward(reward: i64) -> TableIter<Self> {
        Self::filter(|row| row.reward == reward)
    }
    #[allow(unused)]
    pub fn filter_by_complete(complete: bool) -> TableIter<Self> {
        Self::filter(|row| row.complete == complete)
    }
}
