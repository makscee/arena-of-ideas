// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ContentRotationArgs {}

impl From<ContentRotationArgs> for super::Reducer {
    fn from(args: ContentRotationArgs) -> Self {
        Self::ContentRotation
    }
}

impl __sdk::InModule for ContentRotationArgs {
    type Module = super::RemoteModule;
}

pub struct ContentRotationCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `content_rotation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait content_rotation {
    /// Request that the remote module invoke the reducer `content_rotation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_content_rotation`] callbacks.
    fn content_rotation(&self) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `content_rotation`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`ContentRotationCallbackId`] can be passed to [`Self::remove_on_content_rotation`]
    /// to cancel the callback.
    fn on_content_rotation(
        &self,
        callback: impl FnMut(&super::ReducerEventContext) + Send + 'static,
    ) -> ContentRotationCallbackId;
    /// Cancel a callback previously registered by [`Self::on_content_rotation`],
    /// causing it not to run in the future.
    fn remove_on_content_rotation(&self, callback: ContentRotationCallbackId);
}

impl content_rotation for super::RemoteReducers {
    fn content_rotation(&self) -> __sdk::Result<()> {
        self.imp
            .call_reducer("content_rotation", ContentRotationArgs {})
    }
    fn on_content_rotation(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext) + Send + 'static,
    ) -> ContentRotationCallbackId {
        ContentRotationCallbackId(self.imp.on_reducer(
            "content_rotation",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::ContentRotation {},
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx)
            }),
        ))
    }
    fn remove_on_content_rotation(&self, callback: ContentRotationCallbackId) {
        self.imp.remove_on_reducer("content_rotation", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `content_rotation`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_content_rotation {
    /// Set the call-reducer flags for the reducer `content_rotation` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn content_rotation(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_content_rotation for super::SetReducerFlags {
    fn content_rotation(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("content_rotation", flags);
    }
}
