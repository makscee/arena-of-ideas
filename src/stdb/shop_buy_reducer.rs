// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub struct ShopBuy {
    pub slot: u8,
}

impl __sdk::spacetime_module::InModule for ShopBuy {
    type Module = super::RemoteModule;
}

pub struct ShopBuyCallbackId(__sdk::callbacks::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `shop_buy`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait shop_buy {
    /// Request that the remote module invoke the reducer `shop_buy` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_shop_buy`] callbacks.
    fn shop_buy(&self, slot: u8) -> __anyhow::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `shop_buy`.
    ///
    /// The [`super::EventContext`] passed to the `callback`
    /// will always have [`__sdk::Event::Reducer`] as its `event`,
    /// but it may or may not have terminated successfully and been committed.
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::EventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`ShopBuyCallbackId`] can be passed to [`Self::remove_on_shop_buy`]
    /// to cancel the callback.
    fn on_shop_buy(
        &self,
        callback: impl FnMut(&super::EventContext, &u8) + Send + 'static,
    ) -> ShopBuyCallbackId;
    /// Cancel a callback previously registered by [`Self::on_shop_buy`],
    /// causing it not to run in the future.
    fn remove_on_shop_buy(&self, callback: ShopBuyCallbackId);
}

impl shop_buy for super::RemoteReducers {
    fn shop_buy(&self, slot: u8) -> __anyhow::Result<()> {
        self.imp.call_reducer("shop_buy", ShopBuy { slot })
    }
    fn on_shop_buy(
        &self,
        mut callback: impl FnMut(&super::EventContext, &u8) + Send + 'static,
    ) -> ShopBuyCallbackId {
        ShopBuyCallbackId(self.imp.on_reducer::<ShopBuy>(
            "shop_buy",
            Box::new(move |ctx: &super::EventContext, args: &ShopBuy| callback(ctx, &args.slot)),
        ))
    }
    fn remove_on_shop_buy(&self, callback: ShopBuyCallbackId) {
        self.imp
            .remove_on_reducer::<ShopBuy>("shop_buy", callback.0)
    }
}
