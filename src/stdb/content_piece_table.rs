// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::s_content_type_type::SContentType;
use super::t_content_piece_type::TContentPiece;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `content_piece`.
///
/// Obtain a handle from the [`ContentPieceTableAccess::content_piece`] method on [`super::RemoteTables`],
/// like `ctx.db.content_piece()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_piece().on_insert(...)`.
pub struct ContentPieceTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TContentPiece>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `content_piece`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ContentPieceTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ContentPieceTableHandle`], which mediates access to the table `content_piece`.
    fn content_piece(&self) -> ContentPieceTableHandle<'_>;
}

impl ContentPieceTableAccess for super::RemoteTables {
    fn content_piece(&self) -> ContentPieceTableHandle<'_> {
        ContentPieceTableHandle {
            imp: self.imp.get_table::<TContentPiece>("content_piece"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ContentPieceInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct ContentPieceDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for ContentPieceTableHandle<'ctx> {
    type Row = TContentPiece;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TContentPiece> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ContentPieceInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentPieceInsertCallbackId {
        ContentPieceInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ContentPieceInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ContentPieceDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ContentPieceDeleteCallbackId {
        ContentPieceDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ContentPieceDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ContentPieceUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for ContentPieceTableHandle<'ctx> {
    type UpdateCallbackId = ContentPieceUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ContentPieceUpdateCallbackId {
        ContentPieceUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ContentPieceUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TContentPiece>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TContentPiece| &row.id,
    )
    .context("Failed to parse table update for table \"content_piece\"")
}

/// Access to the `id` unique index on the table `content_piece`,
/// which allows point queries on the field of the same name
/// via the [`ContentPieceIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_piece().id().find(...)`.
pub struct ContentPieceIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TContentPiece, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ContentPieceTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `content_piece`.
    pub fn id(&self) -> ContentPieceIdUnique<'ctx> {
        ContentPieceIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ContentPieceIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TContentPiece> {
        self.imp.find(col_val)
    }
}

/// Access to the `data` unique index on the table `content_piece`,
/// which allows point queries on the field of the same name
/// via the [`ContentPieceDataUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.content_piece().data().find(...)`.
pub struct ContentPieceDataUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TContentPiece, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ContentPieceTableHandle<'ctx> {
    /// Get a handle on the `data` unique index on the table `content_piece`.
    pub fn data(&self) -> ContentPieceDataUnique<'ctx> {
        ContentPieceDataUnique {
            imp: self
                .imp
                .get_unique_constraint::<String>("data", |row| &row.data),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ContentPieceDataUnique<'ctx> {
    /// Find the subscribed row whose `data` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<TContentPiece> {
        self.imp.find(col_val)
    }
}
