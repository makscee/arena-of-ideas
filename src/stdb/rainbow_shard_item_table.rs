// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN RUST INSTEAD.

#![allow(unused)]
use super::t_rainbow_shard_item_type::TRainbowShardItem;
use spacetimedb_sdk::{
    self as __sdk,
    anyhow::{self as __anyhow, Context as _},
    lib as __lib, sats as __sats, ws_messages as __ws,
};

/// Table handle for the table `rainbow_shard_item`.
///
/// Obtain a handle from the [`RainbowShardItemTableAccess::rainbow_shard_item`] method on [`super::RemoteTables`],
/// like `ctx.db.rainbow_shard_item()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.rainbow_shard_item().on_insert(...)`.
pub struct RainbowShardItemTableHandle<'ctx> {
    imp: __sdk::db_connection::TableHandle<TRainbowShardItem>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `rainbow_shard_item`.
///
/// Implemented for [`super::RemoteTables`].
pub trait RainbowShardItemTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`RainbowShardItemTableHandle`], which mediates access to the table `rainbow_shard_item`.
    fn rainbow_shard_item(&self) -> RainbowShardItemTableHandle<'_>;
}

impl RainbowShardItemTableAccess for super::RemoteTables {
    fn rainbow_shard_item(&self) -> RainbowShardItemTableHandle<'_> {
        RainbowShardItemTableHandle {
            imp: self
                .imp
                .get_table::<TRainbowShardItem>("rainbow_shard_item"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct RainbowShardItemInsertCallbackId(__sdk::callbacks::CallbackId);
pub struct RainbowShardItemDeleteCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::Table for RainbowShardItemTableHandle<'ctx> {
    type Row = TRainbowShardItem;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TRainbowShardItem> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = RainbowShardItemInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> RainbowShardItemInsertCallbackId {
        RainbowShardItemInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: RainbowShardItemInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = RainbowShardItemDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> RainbowShardItemDeleteCallbackId {
        RainbowShardItemDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: RainbowShardItemDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct RainbowShardItemUpdateCallbackId(__sdk::callbacks::CallbackId);

impl<'ctx> __sdk::table::TableWithPrimaryKey for RainbowShardItemTableHandle<'ctx> {
    type UpdateCallbackId = RainbowShardItemUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> RainbowShardItemUpdateCallbackId {
        RainbowShardItemUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: RainbowShardItemUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __anyhow::Result<__sdk::spacetime_module::TableUpdate<TRainbowShardItem>> {
    __sdk::spacetime_module::TableUpdate::parse_table_update_with_primary_key::<u64>(
        raw_updates,
        |row: &TRainbowShardItem| &row.id,
    )
    .context("Failed to parse table update for table \"rainbow_shard_item\"")
}

/// Access to the `id` unique index on the table `rainbow_shard_item`,
/// which allows point queries on the field of the same name
/// via the [`RainbowShardItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.rainbow_shard_item().id().find(...)`.
pub struct RainbowShardItemIdUnique<'ctx> {
    imp: __sdk::client_cache::UniqueConstraint<TRainbowShardItem, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> RainbowShardItemTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `rainbow_shard_item`.
    pub fn id(&self) -> RainbowShardItemIdUnique<'ctx> {
        RainbowShardItemIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id", |row| &row.id),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> RainbowShardItemIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TRainbowShardItem> {
        self.imp.find(col_val)
    }
}
