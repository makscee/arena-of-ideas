use super::*;

pub struct WalkthroughSystem {
}

impl WalkthroughSystem {
    pub fn run_simulation(world: &mut legion::World, resources: &mut Resources) {
        let pool = resources.hero_pool.all();

    }

    
}