(
    [
        Hollow(F(1.0)),
        Color(C("#2979FF")),
        Alpha(F(0.1)),
        Repeat(
            I(1000),
            List(
                [
                    Rectangle(F(0.5)),
                    Rotate(Sum(GT, Var(index))),
                    Translate(Mul(F(0.2), Cos(Sum(GT, Var(index))))),
                    Paint,
                ],
            ),
        ),
    ],
)