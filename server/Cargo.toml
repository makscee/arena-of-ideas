[package]
name = "server"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
shared = { path = "../shared" }
spacetimedb = "2.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
