// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchStackUnitArgs {
    pub unit_id: u64,
    pub target_unit_id: u64,
}

impl From<MatchStackUnitArgs> for super::Reducer {
    fn from(args: MatchStackUnitArgs) -> Self {
        Self::MatchStackUnit {
            unit_id: args.unit_id,
            target_unit_id: args.target_unit_id,
        }
    }
}

impl __sdk::InModule for MatchStackUnitArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_stack_unit`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_stack_unit {
    /// Request that the remote module invoke the reducer `match_stack_unit` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`match_stack_unit:match_stack_unit_then`] to run a callback after the reducer completes.
    fn match_stack_unit(&self, unit_id: u64, target_unit_id: u64) -> __sdk::Result<()> {
        self.match_stack_unit_then(unit_id, target_unit_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `match_stack_unit` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn match_stack_unit_then(
        &self,
        unit_id: u64,
        target_unit_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl match_stack_unit for super::RemoteReducers {
    fn match_stack_unit_then(
        &self,
        unit_id: u64,
        target_unit_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MatchStackUnitArgs {
                unit_id,
                target_unit_id,
            },
            callback,
        )
    }
}
