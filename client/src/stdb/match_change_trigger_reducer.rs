// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchChangeTriggerArgs {
    pub fusion_id: u64,
    pub trigger_unit: u64,
}

impl From<MatchChangeTriggerArgs> for super::Reducer {
    fn from(args: MatchChangeTriggerArgs) -> Self {
        Self::MatchChangeTrigger {
            fusion_id: args.fusion_id,
            trigger_unit: args.trigger_unit,
        }
    }
}

impl __sdk::InModule for MatchChangeTriggerArgs {
    type Module = super::RemoteModule;
}

pub struct MatchChangeTriggerCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_change_trigger`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_change_trigger {
    /// Request that the remote module invoke the reducer `match_change_trigger` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_match_change_trigger`] callbacks.
    fn match_change_trigger(&self, fusion_id: u64, trigger_unit: u64) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `match_change_trigger`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`MatchChangeTriggerCallbackId`] can be passed to [`Self::remove_on_match_change_trigger`]
    /// to cancel the callback.
    fn on_match_change_trigger(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &u64) + Send + 'static,
    ) -> MatchChangeTriggerCallbackId;
    /// Cancel a callback previously registered by [`Self::on_match_change_trigger`],
    /// causing it not to run in the future.
    fn remove_on_match_change_trigger(&self, callback: MatchChangeTriggerCallbackId);
}

impl match_change_trigger for super::RemoteReducers {
    fn match_change_trigger(&self, fusion_id: u64, trigger_unit: u64) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "match_change_trigger",
            MatchChangeTriggerArgs {
                fusion_id,
                trigger_unit,
            },
        )
    }
    fn on_match_change_trigger(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &u64) + Send + 'static,
    ) -> MatchChangeTriggerCallbackId {
        MatchChangeTriggerCallbackId(self.imp.on_reducer(
            "match_change_trigger",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::MatchChangeTrigger {
                                    fusion_id,
                                    trigger_unit,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, fusion_id, trigger_unit)
            }),
        ))
    }
    fn remove_on_match_change_trigger(&self, callback: MatchChangeTriggerCallbackId) {
        self.imp
            .remove_on_reducer("match_change_trigger", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `match_change_trigger`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_match_change_trigger {
    /// Set the call-reducer flags for the reducer `match_change_trigger` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn match_change_trigger(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_match_change_trigger for super::SetReducerFlags {
    fn match_change_trigger(&self, flags: __ws::CallReducerFlags) {
        self.imp
            .set_call_reducer_flags("match_change_trigger", flags);
    }
}
