// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 2.1.0 (commit 6981f48b4bc1a71c8dd9bdfe5a2c343f6370243d).

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

pub mod admin_add_gold_reducer;
pub mod admin_add_votes_reducer;
pub mod admin_daily_update_reducer;
pub mod admin_delete_node_recursive_reducer;
pub mod admin_edit_nodes_reducer;
pub mod admin_edit_owner_reducer;
pub mod admin_upload_world_reducer;
pub mod battle_table;
pub mod content_delete_node_reducer;
pub mod content_downvote_node_reducer;
pub mod content_publish_node_reducer;
pub mod content_reset_core_reducer;
pub mod content_suggest_node_reducer;
pub mod content_upvote_node_reducer;
pub mod creation_parts_table;
pub mod creators_table;
pub mod daily_update_timer_table;
pub mod daily_update_timer_type;
pub mod global_data_table;
pub mod global_data_type;
pub mod global_settings_table;
pub mod global_settings_type;
pub mod login_by_identity_reducer;
pub mod logout_reducer;
pub mod match_abandon_reducer;
pub mod match_bench_unit_reducer;
pub mod match_boss_battle_reducer;
pub mod match_cancel_fusion_reducer;
pub mod match_choose_fusion_reducer;
pub mod match_complete_reducer;
pub mod match_insert_reducer;
pub mod match_move_unit_reducer;
pub mod match_sell_unit_reducer;
pub mod match_settings_type;
pub mod match_shop_buy_reducer;
pub mod match_shop_reroll_reducer;
pub mod match_stack_unit_reducer;
pub mod match_start_battle_reducer;
pub mod match_start_fusion_reducer;
pub mod match_submit_battle_result_reducer;
pub mod node_links_table;
pub mod nodes_world_table;
pub mod register_reducer;
pub mod t_battle_type;
pub mod t_creation_parts_type;
pub mod t_creators_type;
pub mod t_node_link_type;
pub mod t_node_type;
pub mod t_votes_history_type;
pub mod t_votes_type;
pub mod votes_history_table;
pub mod votes_table;

pub use admin_add_gold_reducer::admin_add_gold;
pub use admin_add_votes_reducer::admin_add_votes;
pub use admin_daily_update_reducer::admin_daily_update;
pub use admin_delete_node_recursive_reducer::admin_delete_node_recursive;
pub use admin_edit_nodes_reducer::admin_edit_nodes;
pub use admin_edit_owner_reducer::admin_edit_owner;
pub use admin_upload_world_reducer::admin_upload_world;
pub use battle_table::*;
pub use content_delete_node_reducer::content_delete_node;
pub use content_downvote_node_reducer::content_downvote_node;
pub use content_publish_node_reducer::content_publish_node;
pub use content_reset_core_reducer::content_reset_core;
pub use content_suggest_node_reducer::content_suggest_node;
pub use content_upvote_node_reducer::content_upvote_node;
pub use creation_parts_table::*;
pub use creators_table::*;
pub use daily_update_timer_table::*;
pub use daily_update_timer_type::DailyUpdateTimer;
pub use global_data_table::*;
pub use global_data_type::GlobalData;
pub use global_settings_table::*;
pub use global_settings_type::GlobalSettings;
pub use login_by_identity_reducer::login_by_identity;
pub use logout_reducer::logout;
pub use match_abandon_reducer::match_abandon;
pub use match_bench_unit_reducer::match_bench_unit;
pub use match_boss_battle_reducer::match_boss_battle;
pub use match_cancel_fusion_reducer::match_cancel_fusion;
pub use match_choose_fusion_reducer::match_choose_fusion;
pub use match_complete_reducer::match_complete;
pub use match_insert_reducer::match_insert;
pub use match_move_unit_reducer::match_move_unit;
pub use match_sell_unit_reducer::match_sell_unit;
pub use match_settings_type::MatchSettings;
pub use match_shop_buy_reducer::match_shop_buy;
pub use match_shop_reroll_reducer::match_shop_reroll;
pub use match_stack_unit_reducer::match_stack_unit;
pub use match_start_battle_reducer::match_start_battle;
pub use match_start_fusion_reducer::match_start_fusion;
pub use match_submit_battle_result_reducer::match_submit_battle_result;
pub use node_links_table::*;
pub use nodes_world_table::*;
pub use register_reducer::register;
pub use t_battle_type::TBattle;
pub use t_creation_parts_type::TCreationParts;
pub use t_creators_type::TCreators;
pub use t_node_link_type::TNodeLink;
pub use t_node_type::TNode;
pub use t_votes_history_type::TVotesHistory;
pub use t_votes_type::TVotes;
pub use votes_history_table::*;
pub use votes_table::*;

#[derive(Clone, PartialEq, Debug)]

/// One of the reducers defined by this module.
///
/// Contained within a [`__sdk::ReducerEvent`] in [`EventContext`]s for reducer events
/// to indicate which reducer caused the event.

pub enum Reducer {
    AdminAddGold,
    AdminAddVotes {
        amount: i32,
    },
    AdminDailyUpdate,
    AdminDeleteNodeRecursive {
        id: u64,
    },
    AdminEditNodes {
        pack: String,
    },
    AdminEditOwner {
        node_id: u64,
        owner_id: u64,
    },
    AdminUploadWorld {
        global_settings: GlobalSettings,
        nodes: Vec<String>,
        links: Vec<String>,
    },
    ContentDeleteNode {
        node_id: u64,
    },
    ContentDownvoteNode {
        node_id: u64,
    },
    ContentPublishNode {
        pack: String,
        parent: Option<u64>,
    },
    ContentResetCore,
    ContentSuggestNode {
        kind: String,
        data: String,
        parent: Option<u64>,
    },
    ContentUpvoteNode {
        node_id: u64,
    },
    LoginByIdentity,
    Logout,
    MatchAbandon,
    MatchBenchUnit {
        unit_id: u64,
    },
    MatchBossBattle,
    MatchCancelFusion,
    MatchChooseFusion {
        fusion_choice: String,
    },
    MatchComplete,
    MatchInsert,
    MatchMoveUnit {
        unit_id: u64,
        slot_index: i32,
    },
    MatchSellUnit {
        unit_id: u64,
    },
    MatchShopBuy {
        shop_idx: u8,
    },
    MatchShopReroll,
    MatchStackUnit {
        unit_id: u64,
        target_unit_id: u64,
    },
    MatchStartBattle,
    MatchStartFusion {
        source_id: u64,
        target_id: u64,
    },
    MatchSubmitBattleResult {
        id: u64,
        result: bool,
        hash: u64,
    },
    Register {
        name: String,
    },
}

impl __sdk::InModule for Reducer {
    type Module = RemoteModule;
}

impl __sdk::Reducer for Reducer {
    fn reducer_name(&self) -> &'static str {
        match self {
            Reducer::AdminAddGold => "admin_add_gold",
            Reducer::AdminAddVotes { .. } => "admin_add_votes",
            Reducer::AdminDailyUpdate => "admin_daily_update",
            Reducer::AdminDeleteNodeRecursive { .. } => "admin_delete_node_recursive",
            Reducer::AdminEditNodes { .. } => "admin_edit_nodes",
            Reducer::AdminEditOwner { .. } => "admin_edit_owner",
            Reducer::AdminUploadWorld { .. } => "admin_upload_world",
            Reducer::ContentDeleteNode { .. } => "content_delete_node",
            Reducer::ContentDownvoteNode { .. } => "content_downvote_node",
            Reducer::ContentPublishNode { .. } => "content_publish_node",
            Reducer::ContentResetCore => "content_reset_core",
            Reducer::ContentSuggestNode { .. } => "content_suggest_node",
            Reducer::ContentUpvoteNode { .. } => "content_upvote_node",
            Reducer::LoginByIdentity => "login_by_identity",
            Reducer::Logout => "logout",
            Reducer::MatchAbandon => "match_abandon",
            Reducer::MatchBenchUnit { .. } => "match_bench_unit",
            Reducer::MatchBossBattle => "match_boss_battle",
            Reducer::MatchCancelFusion => "match_cancel_fusion",
            Reducer::MatchChooseFusion { .. } => "match_choose_fusion",
            Reducer::MatchComplete => "match_complete",
            Reducer::MatchInsert => "match_insert",
            Reducer::MatchMoveUnit { .. } => "match_move_unit",
            Reducer::MatchSellUnit { .. } => "match_sell_unit",
            Reducer::MatchShopBuy { .. } => "match_shop_buy",
            Reducer::MatchShopReroll => "match_shop_reroll",
            Reducer::MatchStackUnit { .. } => "match_stack_unit",
            Reducer::MatchStartBattle => "match_start_battle",
            Reducer::MatchStartFusion { .. } => "match_start_fusion",
            Reducer::MatchSubmitBattleResult { .. } => "match_submit_battle_result",
            Reducer::Register { .. } => "register",
            _ => unreachable!(),
        }
    }
    #[allow(clippy::clone_on_copy)]
    fn args_bsatn(&self) -> Result<Vec<u8>, __sats::bsatn::EncodeError> {
        match self {
            Reducer::AdminAddGold => {
                __sats::bsatn::to_vec(&admin_add_gold_reducer::AdminAddGoldArgs {})
            }
            Reducer::AdminAddVotes { amount } => {
                __sats::bsatn::to_vec(&admin_add_votes_reducer::AdminAddVotesArgs {
                    amount: amount.clone(),
                })
            }
            Reducer::AdminDailyUpdate => {
                __sats::bsatn::to_vec(&admin_daily_update_reducer::AdminDailyUpdateArgs {})
            }
            Reducer::AdminDeleteNodeRecursive { id } => __sats::bsatn::to_vec(
                &admin_delete_node_recursive_reducer::AdminDeleteNodeRecursiveArgs {
                    id: id.clone(),
                },
            ),
            Reducer::AdminEditNodes { pack } => {
                __sats::bsatn::to_vec(&admin_edit_nodes_reducer::AdminEditNodesArgs {
                    pack: pack.clone(),
                })
            }
            Reducer::AdminEditOwner { node_id, owner_id } => {
                __sats::bsatn::to_vec(&admin_edit_owner_reducer::AdminEditOwnerArgs {
                    node_id: node_id.clone(),
                    owner_id: owner_id.clone(),
                })
            }
            Reducer::AdminUploadWorld {
                global_settings,
                nodes,
                links,
            } => __sats::bsatn::to_vec(&admin_upload_world_reducer::AdminUploadWorldArgs {
                global_settings: global_settings.clone(),
                nodes: nodes.clone(),
                links: links.clone(),
            }),
            Reducer::ContentDeleteNode { node_id } => {
                __sats::bsatn::to_vec(&content_delete_node_reducer::ContentDeleteNodeArgs {
                    node_id: node_id.clone(),
                })
            }
            Reducer::ContentDownvoteNode { node_id } => {
                __sats::bsatn::to_vec(&content_downvote_node_reducer::ContentDownvoteNodeArgs {
                    node_id: node_id.clone(),
                })
            }
            Reducer::ContentPublishNode { pack, parent } => {
                __sats::bsatn::to_vec(&content_publish_node_reducer::ContentPublishNodeArgs {
                    pack: pack.clone(),
                    parent: parent.clone(),
                })
            }
            Reducer::ContentResetCore => {
                __sats::bsatn::to_vec(&content_reset_core_reducer::ContentResetCoreArgs {})
            }
            Reducer::ContentSuggestNode { kind, data, parent } => {
                __sats::bsatn::to_vec(&content_suggest_node_reducer::ContentSuggestNodeArgs {
                    kind: kind.clone(),
                    data: data.clone(),
                    parent: parent.clone(),
                })
            }
            Reducer::ContentUpvoteNode { node_id } => {
                __sats::bsatn::to_vec(&content_upvote_node_reducer::ContentUpvoteNodeArgs {
                    node_id: node_id.clone(),
                })
            }
            Reducer::LoginByIdentity => {
                __sats::bsatn::to_vec(&login_by_identity_reducer::LoginByIdentityArgs {})
            }
            Reducer::Logout => __sats::bsatn::to_vec(&logout_reducer::LogoutArgs {}),
            Reducer::MatchAbandon => {
                __sats::bsatn::to_vec(&match_abandon_reducer::MatchAbandonArgs {})
            }
            Reducer::MatchBenchUnit { unit_id } => {
                __sats::bsatn::to_vec(&match_bench_unit_reducer::MatchBenchUnitArgs {
                    unit_id: unit_id.clone(),
                })
            }
            Reducer::MatchBossBattle => {
                __sats::bsatn::to_vec(&match_boss_battle_reducer::MatchBossBattleArgs {})
            }
            Reducer::MatchCancelFusion => {
                __sats::bsatn::to_vec(&match_cancel_fusion_reducer::MatchCancelFusionArgs {})
            }
            Reducer::MatchChooseFusion { fusion_choice } => {
                __sats::bsatn::to_vec(&match_choose_fusion_reducer::MatchChooseFusionArgs {
                    fusion_choice: fusion_choice.clone(),
                })
            }
            Reducer::MatchComplete => {
                __sats::bsatn::to_vec(&match_complete_reducer::MatchCompleteArgs {})
            }
            Reducer::MatchInsert => {
                __sats::bsatn::to_vec(&match_insert_reducer::MatchInsertArgs {})
            }
            Reducer::MatchMoveUnit {
                unit_id,
                slot_index,
            } => __sats::bsatn::to_vec(&match_move_unit_reducer::MatchMoveUnitArgs {
                unit_id: unit_id.clone(),
                slot_index: slot_index.clone(),
            }),
            Reducer::MatchSellUnit { unit_id } => {
                __sats::bsatn::to_vec(&match_sell_unit_reducer::MatchSellUnitArgs {
                    unit_id: unit_id.clone(),
                })
            }
            Reducer::MatchShopBuy { shop_idx } => {
                __sats::bsatn::to_vec(&match_shop_buy_reducer::MatchShopBuyArgs {
                    shop_idx: shop_idx.clone(),
                })
            }
            Reducer::MatchShopReroll => {
                __sats::bsatn::to_vec(&match_shop_reroll_reducer::MatchShopRerollArgs {})
            }
            Reducer::MatchStackUnit {
                unit_id,
                target_unit_id,
            } => __sats::bsatn::to_vec(&match_stack_unit_reducer::MatchStackUnitArgs {
                unit_id: unit_id.clone(),
                target_unit_id: target_unit_id.clone(),
            }),
            Reducer::MatchStartBattle => {
                __sats::bsatn::to_vec(&match_start_battle_reducer::MatchStartBattleArgs {})
            }
            Reducer::MatchStartFusion {
                source_id,
                target_id,
            } => __sats::bsatn::to_vec(&match_start_fusion_reducer::MatchStartFusionArgs {
                source_id: source_id.clone(),
                target_id: target_id.clone(),
            }),
            Reducer::MatchSubmitBattleResult { id, result, hash } => __sats::bsatn::to_vec(
                &match_submit_battle_result_reducer::MatchSubmitBattleResultArgs {
                    id: id.clone(),
                    result: result.clone(),
                    hash: hash.clone(),
                },
            ),
            Reducer::Register { name } => {
                __sats::bsatn::to_vec(&register_reducer::RegisterArgs { name: name.clone() })
            }
            _ => unreachable!(),
        }
    }
}

#[derive(Default, Debug)]
#[allow(non_snake_case)]
#[doc(hidden)]
pub struct DbUpdate {
    battle: __sdk::TableUpdate<TBattle>,
    creation_parts: __sdk::TableUpdate<TCreationParts>,
    creators: __sdk::TableUpdate<TCreators>,
    daily_update_timer: __sdk::TableUpdate<DailyUpdateTimer>,
    global_data: __sdk::TableUpdate<GlobalData>,
    global_settings: __sdk::TableUpdate<GlobalSettings>,
    node_links: __sdk::TableUpdate<TNodeLink>,
    nodes_world: __sdk::TableUpdate<TNode>,
    votes: __sdk::TableUpdate<TVotes>,
    votes_history: __sdk::TableUpdate<TVotesHistory>,
}

impl TryFrom<__ws::v2::TransactionUpdate> for DbUpdate {
    type Error = __sdk::Error;
    fn try_from(raw: __ws::v2::TransactionUpdate) -> Result<Self, Self::Error> {
        let mut db_update = DbUpdate::default();
        for table_update in __sdk::transaction_update_iter_table_updates(raw) {
            match &table_update.table_name[..] {
                "battle" => db_update
                    .battle
                    .append(battle_table::parse_table_update(table_update)?),
                "creation_parts" => db_update
                    .creation_parts
                    .append(creation_parts_table::parse_table_update(table_update)?),
                "creators" => db_update
                    .creators
                    .append(creators_table::parse_table_update(table_update)?),
                "daily_update_timer" => db_update
                    .daily_update_timer
                    .append(daily_update_timer_table::parse_table_update(table_update)?),
                "global_data" => db_update
                    .global_data
                    .append(global_data_table::parse_table_update(table_update)?),
                "global_settings" => db_update
                    .global_settings
                    .append(global_settings_table::parse_table_update(table_update)?),
                "node_links" => db_update
                    .node_links
                    .append(node_links_table::parse_table_update(table_update)?),
                "nodes_world" => db_update
                    .nodes_world
                    .append(nodes_world_table::parse_table_update(table_update)?),
                "votes" => db_update
                    .votes
                    .append(votes_table::parse_table_update(table_update)?),
                "votes_history" => db_update
                    .votes_history
                    .append(votes_history_table::parse_table_update(table_update)?),

                unknown => {
                    return Err(__sdk::InternalError::unknown_name(
                        "table",
                        unknown,
                        "DatabaseUpdate",
                    )
                    .into());
                }
            }
        }
        Ok(db_update)
    }
}

impl __sdk::InModule for DbUpdate {
    type Module = RemoteModule;
}

impl __sdk::DbUpdate for DbUpdate {
    fn apply_to_client_cache(
        &self,
        cache: &mut __sdk::ClientCache<RemoteModule>,
    ) -> AppliedDiff<'_> {
        let mut diff = AppliedDiff::default();

        diff.battle = cache
            .apply_diff_to_table::<TBattle>("battle", &self.battle)
            .with_updates_by_pk(|row| &row.id);
        diff.creation_parts = cache
            .apply_diff_to_table::<TCreationParts>("creation_parts", &self.creation_parts)
            .with_updates_by_pk(|row| &row.node_id);
        diff.creators = cache
            .apply_diff_to_table::<TCreators>("creators", &self.creators)
            .with_updates_by_pk(|row| &row.node_id);
        diff.daily_update_timer = cache
            .apply_diff_to_table::<DailyUpdateTimer>("daily_update_timer", &self.daily_update_timer)
            .with_updates_by_pk(|row| &row.scheduled_id);
        diff.global_data = cache
            .apply_diff_to_table::<GlobalData>("global_data", &self.global_data)
            .with_updates_by_pk(|row| &row.always_zero);
        diff.global_settings = cache
            .apply_diff_to_table::<GlobalSettings>("global_settings", &self.global_settings)
            .with_updates_by_pk(|row| &row.always_zero);
        diff.node_links = cache
            .apply_diff_to_table::<TNodeLink>("node_links", &self.node_links)
            .with_updates_by_pk(|row| &row.id);
        diff.nodes_world = cache
            .apply_diff_to_table::<TNode>("nodes_world", &self.nodes_world)
            .with_updates_by_pk(|row| &row.id);
        diff.votes = cache
            .apply_diff_to_table::<TVotes>("votes", &self.votes)
            .with_updates_by_pk(|row| &row.player_id);
        diff.votes_history = cache
            .apply_diff_to_table::<TVotesHistory>("votes_history", &self.votes_history)
            .with_updates_by_pk(|row| &row.id);

        diff
    }
    fn parse_initial_rows(raw: __ws::v2::QueryRows) -> __sdk::Result<Self> {
        let mut db_update = DbUpdate::default();
        for table_rows in raw.tables {
            match &table_rows.table[..] {
                "battle" => db_update
                    .battle
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "creation_parts" => db_update
                    .creation_parts
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "creators" => db_update
                    .creators
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "daily_update_timer" => db_update
                    .daily_update_timer
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "global_data" => db_update
                    .global_data
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "global_settings" => db_update
                    .global_settings
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "node_links" => db_update
                    .node_links
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "nodes_world" => db_update
                    .nodes_world
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "votes" => db_update
                    .votes
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                "votes_history" => db_update
                    .votes_history
                    .append(__sdk::parse_row_list_as_inserts(table_rows.rows)?),
                unknown => {
                    return Err(
                        __sdk::InternalError::unknown_name("table", unknown, "QueryRows").into(),
                    );
                }
            }
        }
        Ok(db_update)
    }
    fn parse_unsubscribe_rows(raw: __ws::v2::QueryRows) -> __sdk::Result<Self> {
        let mut db_update = DbUpdate::default();
        for table_rows in raw.tables {
            match &table_rows.table[..] {
                "battle" => db_update
                    .battle
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "creation_parts" => db_update
                    .creation_parts
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "creators" => db_update
                    .creators
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "daily_update_timer" => db_update
                    .daily_update_timer
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "global_data" => db_update
                    .global_data
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "global_settings" => db_update
                    .global_settings
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "node_links" => db_update
                    .node_links
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "nodes_world" => db_update
                    .nodes_world
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "votes" => db_update
                    .votes
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                "votes_history" => db_update
                    .votes_history
                    .append(__sdk::parse_row_list_as_deletes(table_rows.rows)?),
                unknown => {
                    return Err(
                        __sdk::InternalError::unknown_name("table", unknown, "QueryRows").into(),
                    );
                }
            }
        }
        Ok(db_update)
    }
}

#[derive(Default)]
#[allow(non_snake_case)]
#[doc(hidden)]
pub struct AppliedDiff<'r> {
    battle: __sdk::TableAppliedDiff<'r, TBattle>,
    creation_parts: __sdk::TableAppliedDiff<'r, TCreationParts>,
    creators: __sdk::TableAppliedDiff<'r, TCreators>,
    daily_update_timer: __sdk::TableAppliedDiff<'r, DailyUpdateTimer>,
    global_data: __sdk::TableAppliedDiff<'r, GlobalData>,
    global_settings: __sdk::TableAppliedDiff<'r, GlobalSettings>,
    node_links: __sdk::TableAppliedDiff<'r, TNodeLink>,
    nodes_world: __sdk::TableAppliedDiff<'r, TNode>,
    votes: __sdk::TableAppliedDiff<'r, TVotes>,
    votes_history: __sdk::TableAppliedDiff<'r, TVotesHistory>,
    __unused: std::marker::PhantomData<&'r ()>,
}

impl __sdk::InModule for AppliedDiff<'_> {
    type Module = RemoteModule;
}

impl<'r> __sdk::AppliedDiff<'r> for AppliedDiff<'r> {
    fn invoke_row_callbacks(
        &self,
        event: &EventContext,
        callbacks: &mut __sdk::DbCallbacks<RemoteModule>,
    ) {
        callbacks.invoke_table_row_callbacks::<TBattle>("battle", &self.battle, event);
        callbacks.invoke_table_row_callbacks::<TCreationParts>(
            "creation_parts",
            &self.creation_parts,
            event,
        );
        callbacks.invoke_table_row_callbacks::<TCreators>("creators", &self.creators, event);
        callbacks.invoke_table_row_callbacks::<DailyUpdateTimer>(
            "daily_update_timer",
            &self.daily_update_timer,
            event,
        );
        callbacks.invoke_table_row_callbacks::<GlobalData>("global_data", &self.global_data, event);
        callbacks.invoke_table_row_callbacks::<GlobalSettings>(
            "global_settings",
            &self.global_settings,
            event,
        );
        callbacks.invoke_table_row_callbacks::<TNodeLink>("node_links", &self.node_links, event);
        callbacks.invoke_table_row_callbacks::<TNode>("nodes_world", &self.nodes_world, event);
        callbacks.invoke_table_row_callbacks::<TVotes>("votes", &self.votes, event);
        callbacks.invoke_table_row_callbacks::<TVotesHistory>(
            "votes_history",
            &self.votes_history,
            event,
        );
    }
}

#[doc(hidden)]
#[derive(Debug)]
pub struct RemoteModule;

impl __sdk::InModule for RemoteModule {
    type Module = Self;
}

/// The `reducers` field of [`EventContext`] and [`DbConnection`],
/// with methods provided by extension traits for each reducer defined by the module.
pub struct RemoteReducers {
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::InModule for RemoteReducers {
    type Module = RemoteModule;
}

/// The `procedures` field of [`DbConnection`] and other [`DbContext`] types,
/// with methods provided by extension traits for each procedure defined by the module.
pub struct RemoteProcedures {
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::InModule for RemoteProcedures {
    type Module = RemoteModule;
}

/// The `db` field of [`EventContext`] and [`DbConnection`],
/// with methods provided by extension traits for each table defined by the module.
pub struct RemoteTables {
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::InModule for RemoteTables {
    type Module = RemoteModule;
}

/// A connection to a remote module, including a materialized view of a subset of the database.
///
/// Connect to a remote module by calling [`DbConnection::builder`]
/// and using the [`__sdk::DbConnectionBuilder`] builder-pattern constructor.
///
/// You must explicitly advance the connection by calling any one of:
///
/// - [`DbConnection::frame_tick`].
#[cfg_attr(not(target_arch = "wasm32"), doc = "- [`DbConnection::run_threaded`].")]
#[cfg_attr(
    target_arch = "wasm32",
    doc = "- [`DbConnection::run_background_task`]."
)]
/// - [`DbConnection::run_async`].
/// - [`DbConnection::advance_one_message`].
#[cfg_attr(
    not(target_arch = "wasm32"),
    doc = "- [`DbConnection::advance_one_message_blocking`]."
)]
/// - [`DbConnection::advance_one_message_async`].
///
/// Which of these methods you should call depends on the specific needs of your application,
/// but you must call one of them, or else the connection will never progress.
pub struct DbConnection {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    #[doc(hidden)]

    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,

    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::InModule for DbConnection {
    type Module = RemoteModule;
}

impl __sdk::DbContext for DbConnection {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl DbConnection {
    /// Builder-pattern constructor for a connection to a remote module.
    ///
    /// See [`__sdk::DbConnectionBuilder`] for required and optional configuration for the new connection.
    pub fn builder() -> __sdk::DbConnectionBuilder<RemoteModule> {
        __sdk::DbConnectionBuilder::new()
    }

    /// If any WebSocket messages are waiting, process one of them.
    ///
    /// Returns `true` if a message was processed, or `false` if the queue is empty.
    /// Callers should invoke this message in a loop until it returns `false`
    /// or for as much time is available to process messages.
    ///
    /// Returns an error if the connection is disconnected.
    /// If the disconnection in question was normal,
    ///  i.e. the result of a call to [`__sdk::DbContext::disconnect`],
    /// the returned error will be downcastable to [`__sdk::DisconnectedError`].
    ///
    /// This is a low-level primitive exposed for power users who need significant control over scheduling.
    /// Most applications should call [`Self::frame_tick`] each frame
    /// to fully exhaust the queue whenever time is available.
    pub fn advance_one_message(&self) -> __sdk::Result<bool> {
        self.imp.advance_one_message()
    }

    /// Process one WebSocket message, potentially blocking the current thread until one is received.
    ///
    /// Returns an error if the connection is disconnected.
    /// If the disconnection in question was normal,
    ///  i.e. the result of a call to [`__sdk::DbContext::disconnect`],
    /// the returned error will be downcastable to [`__sdk::DisconnectedError`].
    ///
    /// This is a low-level primitive exposed for power users who need significant control over scheduling.
    /// Most applications should call [`Self::run_threaded`] to spawn a thread
    /// which advances the connection automatically.
    #[cfg(not(target_arch = "wasm32"))]
    pub fn advance_one_message_blocking(&self) -> __sdk::Result<()> {
        self.imp.advance_one_message_blocking()
    }

    /// Process one WebSocket message, `await`ing until one is received.
    ///
    /// Returns an error if the connection is disconnected.
    /// If the disconnection in question was normal,
    ///  i.e. the result of a call to [`__sdk::DbContext::disconnect`],
    /// the returned error will be downcastable to [`__sdk::DisconnectedError`].
    ///
    /// This is a low-level primitive exposed for power users who need significant control over scheduling.
    /// Most applications should call [`Self::run_async`] to run an `async` loop
    /// which advances the connection when polled.
    pub async fn advance_one_message_async(&self) -> __sdk::Result<()> {
        self.imp.advance_one_message_async().await
    }

    /// Process all WebSocket messages waiting in the queue,
    /// then return without `await`ing or blocking the current thread.
    pub fn frame_tick(&self) -> __sdk::Result<()> {
        self.imp.frame_tick()
    }

    /// Spawn a thread which processes WebSocket messages as they are received.
    #[cfg(not(target_arch = "wasm32"))]
    pub fn run_threaded(&self) -> std::thread::JoinHandle<()> {
        self.imp.run_threaded()
    }

    /// Spawn a background task which processes WebSocket messages as they are received.
    #[cfg(target_arch = "wasm32")]
    pub fn run_background_task(&self) {
        self.imp.run_background_task()
    }

    /// Run an `async` loop which processes WebSocket messages when polled.
    pub async fn run_async(&self) -> __sdk::Result<()> {
        self.imp.run_async().await
    }
}

impl __sdk::DbConnection for DbConnection {
    fn new(imp: __sdk::DbContextImpl<RemoteModule>) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            imp,
        }
    }
}

/// A handle on a subscribed query.
// TODO: Document this better after implementing the new subscription API.
#[derive(Clone)]
pub struct SubscriptionHandle {
    imp: __sdk::SubscriptionHandleImpl<RemoteModule>,
}

impl __sdk::InModule for SubscriptionHandle {
    type Module = RemoteModule;
}

impl __sdk::SubscriptionHandle for SubscriptionHandle {
    fn new(imp: __sdk::SubscriptionHandleImpl<RemoteModule>) -> Self {
        Self { imp }
    }

    /// Returns true if this subscription has been terminated due to an unsubscribe call or an error.
    fn is_ended(&self) -> bool {
        self.imp.is_ended()
    }

    /// Returns true if this subscription has been applied and has not yet been unsubscribed.
    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    /// Unsubscribe from the query controlled by this `SubscriptionHandle`,
    /// then run `on_end` when its rows are removed from the client cache.
    fn unsubscribe_then(self, on_end: __sdk::OnEndedCallback<RemoteModule>) -> __sdk::Result<()> {
        self.imp.unsubscribe_then(Some(on_end))
    }

    fn unsubscribe(self) -> __sdk::Result<()> {
        self.imp.unsubscribe_then(None)
    }
}

/// Alias trait for a [`__sdk::DbContext`] connected to this module,
/// with that trait's associated types bounded to this module's concrete types.
///
/// Users can use this trait as a boundary on definitions which should accept
/// either a [`DbConnection`] or an [`EventContext`] and operate on either.
pub trait RemoteDbContext:
    __sdk::DbContext<
    DbView = RemoteTables,
    Reducers = RemoteReducers,
    SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>,
>
{
}
impl<
        Ctx: __sdk::DbContext<
            DbView = RemoteTables,
            Reducers = RemoteReducers,
            SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>,
        >,
    > RemoteDbContext for Ctx
{
}

/// An [`__sdk::DbContext`] augmented with a [`__sdk::Event`],
/// passed to [`__sdk::Table::on_insert`], [`__sdk::Table::on_delete`] and [`__sdk::TableWithPrimaryKey::on_update`] callbacks.
pub struct EventContext {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,
    /// The event which caused these callbacks to run.
    pub event: __sdk::Event<Reducer>,
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::AbstractEventContext for EventContext {
    type Event = __sdk::Event<Reducer>;
    fn event(&self) -> &Self::Event {
        &self.event
    }
    fn new(imp: __sdk::DbContextImpl<RemoteModule>, event: Self::Event) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            event,
            imp,
        }
    }
}

impl __sdk::InModule for EventContext {
    type Module = RemoteModule;
}

impl __sdk::DbContext for EventContext {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl __sdk::EventContext for EventContext {}

/// An [`__sdk::DbContext`] augmented with a [`__sdk::ReducerEvent`],
/// passed to on-reducer callbacks.
pub struct ReducerEventContext {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,
    /// The event which caused these callbacks to run.
    pub event: __sdk::ReducerEvent<Reducer>,
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::AbstractEventContext for ReducerEventContext {
    type Event = __sdk::ReducerEvent<Reducer>;
    fn event(&self) -> &Self::Event {
        &self.event
    }
    fn new(imp: __sdk::DbContextImpl<RemoteModule>, event: Self::Event) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            event,
            imp,
        }
    }
}

impl __sdk::InModule for ReducerEventContext {
    type Module = RemoteModule;
}

impl __sdk::DbContext for ReducerEventContext {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl __sdk::ReducerEventContext for ReducerEventContext {}

/// An [`__sdk::DbContext`] passed to procedure callbacks.
pub struct ProcedureEventContext {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::AbstractEventContext for ProcedureEventContext {
    type Event = ();
    fn event(&self) -> &Self::Event {
        &()
    }
    fn new(imp: __sdk::DbContextImpl<RemoteModule>, _event: Self::Event) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            imp,
        }
    }
}

impl __sdk::InModule for ProcedureEventContext {
    type Module = RemoteModule;
}

impl __sdk::DbContext for ProcedureEventContext {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl __sdk::ProcedureEventContext for ProcedureEventContext {}

/// An [`__sdk::DbContext`] passed to [`__sdk::SubscriptionBuilder::on_applied`] and [`SubscriptionHandle::unsubscribe_then`] callbacks.
pub struct SubscriptionEventContext {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::AbstractEventContext for SubscriptionEventContext {
    type Event = ();
    fn event(&self) -> &Self::Event {
        &()
    }
    fn new(imp: __sdk::DbContextImpl<RemoteModule>, _event: Self::Event) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            imp,
        }
    }
}

impl __sdk::InModule for SubscriptionEventContext {
    type Module = RemoteModule;
}

impl __sdk::DbContext for SubscriptionEventContext {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl __sdk::SubscriptionEventContext for SubscriptionEventContext {}

/// An [`__sdk::DbContext`] augmented with a [`__sdk::Error`],
/// passed to [`__sdk::DbConnectionBuilder::on_disconnect`], [`__sdk::DbConnectionBuilder::on_connect_error`] and [`__sdk::SubscriptionBuilder::on_error`] callbacks.
pub struct ErrorContext {
    /// Access to tables defined by the module via extension traits implemented for [`RemoteTables`].
    pub db: RemoteTables,
    /// Access to reducers defined by the module via extension traits implemented for [`RemoteReducers`].
    pub reducers: RemoteReducers,
    /// Access to procedures defined by the module via extension traits implemented for [`RemoteProcedures`].
    pub procedures: RemoteProcedures,
    /// The event which caused these callbacks to run.
    pub event: Option<__sdk::Error>,
    imp: __sdk::DbContextImpl<RemoteModule>,
}

impl __sdk::AbstractEventContext for ErrorContext {
    type Event = Option<__sdk::Error>;
    fn event(&self) -> &Self::Event {
        &self.event
    }
    fn new(imp: __sdk::DbContextImpl<RemoteModule>, event: Self::Event) -> Self {
        Self {
            db: RemoteTables { imp: imp.clone() },
            reducers: RemoteReducers { imp: imp.clone() },
            procedures: RemoteProcedures { imp: imp.clone() },
            event,
            imp,
        }
    }
}

impl __sdk::InModule for ErrorContext {
    type Module = RemoteModule;
}

impl __sdk::DbContext for ErrorContext {
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;

    fn db(&self) -> &Self::DbView {
        &self.db
    }
    fn reducers(&self) -> &Self::Reducers {
        &self.reducers
    }
    fn procedures(&self) -> &Self::Procedures {
        &self.procedures
    }

    fn is_active(&self) -> bool {
        self.imp.is_active()
    }

    fn disconnect(&self) -> __sdk::Result<()> {
        self.imp.disconnect()
    }

    type SubscriptionBuilder = __sdk::SubscriptionBuilder<RemoteModule>;

    fn subscription_builder(&self) -> Self::SubscriptionBuilder {
        __sdk::SubscriptionBuilder::new(&self.imp)
    }

    fn try_identity(&self) -> Option<__sdk::Identity> {
        self.imp.try_identity()
    }
    fn connection_id(&self) -> __sdk::ConnectionId {
        self.imp.connection_id()
    }
    fn try_connection_id(&self) -> Option<__sdk::ConnectionId> {
        self.imp.try_connection_id()
    }
}

impl __sdk::ErrorContext for ErrorContext {}

impl __sdk::SpacetimeModule for RemoteModule {
    type DbConnection = DbConnection;
    type EventContext = EventContext;
    type ReducerEventContext = ReducerEventContext;
    type ProcedureEventContext = ProcedureEventContext;
    type SubscriptionEventContext = SubscriptionEventContext;
    type ErrorContext = ErrorContext;
    type Reducer = Reducer;
    type DbView = RemoteTables;
    type Reducers = RemoteReducers;
    type Procedures = RemoteProcedures;
    type DbUpdate = DbUpdate;
    type AppliedDiff<'r> = AppliedDiff<'r>;
    type SubscriptionHandle = SubscriptionHandle;
    type QueryBuilder = __sdk::QueryBuilder;

    fn register_tables(client_cache: &mut __sdk::ClientCache<Self>) {
        battle_table::register_table(client_cache);
        creation_parts_table::register_table(client_cache);
        creators_table::register_table(client_cache);
        daily_update_timer_table::register_table(client_cache);
        global_data_table::register_table(client_cache);
        global_settings_table::register_table(client_cache);
        node_links_table::register_table(client_cache);
        nodes_world_table::register_table(client_cache);
        votes_table::register_table(client_cache);
        votes_history_table::register_table(client_cache);
    }
    const ALL_TABLE_NAMES: &'static [&'static str] = &[
        "battle",
        "creation_parts",
        "creators",
        "daily_update_timer",
        "global_data",
        "global_settings",
        "node_links",
        "nodes_world",
        "votes",
        "votes_history",
    ];
}
