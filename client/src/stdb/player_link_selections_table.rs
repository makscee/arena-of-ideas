// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::t_player_link_selection_type::TPlayerLinkSelection;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `player_link_selections`.
///
/// Obtain a handle from the [`PlayerLinkSelectionsTableAccess::player_link_selections`] method on [`super::RemoteTables`],
/// like `ctx.db.player_link_selections()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.player_link_selections().on_insert(...)`.
pub struct PlayerLinkSelectionsTableHandle<'ctx> {
    imp: __sdk::TableHandle<TPlayerLinkSelection>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `player_link_selections`.
///
/// Implemented for [`super::RemoteTables`].
pub trait PlayerLinkSelectionsTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`PlayerLinkSelectionsTableHandle`], which mediates access to the table `player_link_selections`.
    fn player_link_selections(&self) -> PlayerLinkSelectionsTableHandle<'_>;
}

impl PlayerLinkSelectionsTableAccess for super::RemoteTables {
    fn player_link_selections(&self) -> PlayerLinkSelectionsTableHandle<'_> {
        PlayerLinkSelectionsTableHandle {
            imp: self
                .imp
                .get_table::<TPlayerLinkSelection>("player_link_selections"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct PlayerLinkSelectionsInsertCallbackId(__sdk::CallbackId);
pub struct PlayerLinkSelectionsDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for PlayerLinkSelectionsTableHandle<'ctx> {
    type Row = TPlayerLinkSelection;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TPlayerLinkSelection> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = PlayerLinkSelectionsInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> PlayerLinkSelectionsInsertCallbackId {
        PlayerLinkSelectionsInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: PlayerLinkSelectionsInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = PlayerLinkSelectionsDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> PlayerLinkSelectionsDeleteCallbackId {
        PlayerLinkSelectionsDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: PlayerLinkSelectionsDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TPlayerLinkSelection>("player_link_selections");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}
pub struct PlayerLinkSelectionsUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for PlayerLinkSelectionsTableHandle<'ctx> {
    type UpdateCallbackId = PlayerLinkSelectionsUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> PlayerLinkSelectionsUpdateCallbackId {
        PlayerLinkSelectionsUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: PlayerLinkSelectionsUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<TPlayerLinkSelection>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TPlayerLinkSelection>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `id` unique index on the table `player_link_selections`,
/// which allows point queries on the field of the same name
/// via the [`PlayerLinkSelectionsIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.player_link_selections().id().find(...)`.
pub struct PlayerLinkSelectionsIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TPlayerLinkSelection, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> PlayerLinkSelectionsTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `player_link_selections`.
    pub fn id(&self) -> PlayerLinkSelectionsIdUnique<'ctx> {
        PlayerLinkSelectionsIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> PlayerLinkSelectionsIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TPlayerLinkSelection> {
        self.imp.find(col_val)
    }
}
