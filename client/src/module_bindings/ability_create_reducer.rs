// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::target_type_type::TargetType;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AbilityCreateArgs {
    pub name: String,
    pub description: String,
    pub target_type: TargetType,
    pub effect_script: String,
    pub parent_a: u64,
    pub parent_b: u64,
    pub season: u32,
}

impl From<AbilityCreateArgs> for super::Reducer {
    fn from(args: AbilityCreateArgs) -> Self {
        Self::AbilityCreate {
            name: args.name,
            description: args.description,
            target_type: args.target_type,
            effect_script: args.effect_script,
            parent_a: args.parent_a,
            parent_b: args.parent_b,
            season: args.season,
        }
    }
}

impl __sdk::InModule for AbilityCreateArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ability_create`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ability_create {
    /// Request that the remote module invoke the reducer `ability_create` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ability_create:ability_create_then`] to run a callback after the reducer completes.
    fn ability_create(
        &self,
        name: String,
        description: String,
        target_type: TargetType,
        effect_script: String,
        parent_a: u64,
        parent_b: u64,
        season: u32,
    ) -> __sdk::Result<()> {
        self.ability_create_then(
            name,
            description,
            target_type,
            effect_script,
            parent_a,
            parent_b,
            season,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `ability_create` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ability_create_then(
        &self,
        name: String,
        description: String,
        target_type: TargetType,
        effect_script: String,
        parent_a: u64,
        parent_b: u64,
        season: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl ability_create for super::RemoteReducers {
    fn ability_create_then(
        &self,
        name: String,
        description: String,
        target_type: TargetType,
        effect_script: String,
        parent_a: u64,
        parent_b: u64,
        season: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AbilityCreateArgs {
                name,
                description,
                target_type,
                effect_script,
                parent_a,
                parent_b,
                season,
            },
            callback,
        )
    }
}
