// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GenBreedAbilityArgs {
    pub parent_a_id: u64,
    pub parent_b_id: u64,
    pub prompt: String,
}

impl From<GenBreedAbilityArgs> for super::Reducer {
    fn from(args: GenBreedAbilityArgs) -> Self {
        Self::GenBreedAbility {
            parent_a_id: args.parent_a_id,
            parent_b_id: args.parent_b_id,
            prompt: args.prompt,
        }
    }
}

impl __sdk::InModule for GenBreedAbilityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gen_breed_ability`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gen_breed_ability {
    /// Request that the remote module invoke the reducer `gen_breed_ability` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gen_breed_ability:gen_breed_ability_then`] to run a callback after the reducer completes.
    fn gen_breed_ability(
        &self,
        parent_a_id: u64,
        parent_b_id: u64,
        prompt: String,
    ) -> __sdk::Result<()> {
        self.gen_breed_ability_then(parent_a_id, parent_b_id, prompt, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gen_breed_ability` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gen_breed_ability_then(
        &self,
        parent_a_id: u64,
        parent_b_id: u64,
        prompt: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl gen_breed_ability for super::RemoteReducers {
    fn gen_breed_ability_then(
        &self,
        parent_a_id: u64,
        parent_b_id: u64,
        prompt: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GenBreedAbilityArgs {
                parent_a_id,
                parent_b_id,
                prompt,
            },
            callback,
        )
    }
}
