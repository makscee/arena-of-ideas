// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchFuseUnitsArgs {
    pub slot_a: u32,
    pub slot_b: u32,
    pub trigger_choice: String,
    pub chosen_abilities: Vec<u64>,
}

impl From<MatchFuseUnitsArgs> for super::Reducer {
    fn from(args: MatchFuseUnitsArgs) -> Self {
        Self::MatchFuseUnits {
            slot_a: args.slot_a,
            slot_b: args.slot_b,
            trigger_choice: args.trigger_choice,
            chosen_abilities: args.chosen_abilities,
        }
    }
}

impl __sdk::InModule for MatchFuseUnitsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_fuse_units`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_fuse_units {
    /// Request that the remote module invoke the reducer `match_fuse_units` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`match_fuse_units:match_fuse_units_then`] to run a callback after the reducer completes.
    fn match_fuse_units(
        &self,
        slot_a: u32,
        slot_b: u32,
        trigger_choice: String,
        chosen_abilities: Vec<u64>,
    ) -> __sdk::Result<()> {
        self.match_fuse_units_then(slot_a, slot_b, trigger_choice, chosen_abilities, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `match_fuse_units` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn match_fuse_units_then(
        &self,
        slot_a: u32,
        slot_b: u32,
        trigger_choice: String,
        chosen_abilities: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl match_fuse_units for super::RemoteReducers {
    fn match_fuse_units_then(
        &self,
        slot_a: u32,
        slot_b: u32,
        trigger_choice: String,
        chosen_abilities: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MatchFuseUnitsArgs {
                slot_a,
                slot_b,
                trigger_choice,
                chosen_abilities,
            },
            callback,
        )
    }
}
